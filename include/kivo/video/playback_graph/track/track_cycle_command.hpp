#pragma once

namespace kivo::video::playback_graph {

// Cycle commands are parameterless PlaybackSession APIs:
// cycle_subtitle_track() and cycle_audio_track() return CommandToken.

}  // namespace kivo::video::playback_graph