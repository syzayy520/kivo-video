# KIVO-VIDEO-P5-V2.2 EXECUTION CARD

**Task ID**: KIVO-VIDEO-P5-MEDIA-PLANE-DESIGN-LOCK-V2.2-TASK-BREAKDOWN-001
**Document Type**: execution-card
**Source**: `C:/Users/Administrator/Downloads/Kivo Video P5 Design V2.2 Final Lock Patch.docx`
**Stage**: P5 Video Media Plane Design Lock
**Baseline**: V2.2 Final Lock Patch (supersedes V2.0/V2.1 conflicts; V2.0 unchanged clauses remain valid)
**Status**: READY_FOR_P5A_PLANNING_DRAFT (implementation FORBIDDEN until P5A Planning Draft + P5 Gate Review pass)
**Generated**: 2026-06-28

---

## MERGE RULES (Section 0)

1. V2.2 冲突覆盖优先级最高：V2.2 > V2.1 > V2.0
2. V2.2 未覆盖的 V2.0/V2.1 原章节继续有效
3. V2.1 Delta 中方向正确但未结构化的内容统一并入 V2.2
4. V2.2 后不允许"口号式能力声明"替代可测试验收项
5. P5A 前必须先执行 V2.2 Gate Review（本执行卡）

---

## GATE-001: Capability Registry 统一能力注册表

**Task ID**: KIVO-VIDEO-P5-GATE-001-CAPABILITY-REGISTRY
**Source Section**: §1
**Gate Stage**: P5A (contracts), P5F (runtime), P5H (closure)
**Priority**: P0 – P5 baseline closure gate

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | 12 new types exist as C++ header files | `include/kivo/video_plane/capability/` 目录下有 12 个头文件 |
| E2 | CMake target `kivo_video_plane_contracts` compiles | cmake --build pass, no FFmpeg/D3D11/Qt/WASAPI link |
| E3 | 1.5 节所有能力域有 CapabilitySupportState | 每个域都走 Registry，不各自声明 |
| E4 | P8 读取规则实现为 contract test | P8 只能读 P5CapabilitySnapshot，不能直接读子状态机 |
| E5 | 文档叙述规则 gate 检查通过 | Acceptance gate/release note/diagnostics 带 CapabilitySupportState |

### Required Types (12)

1. **P5CapabilityRegistry** – 统一注册入口
2. **P5CapabilitySnapshot** – 能力快照
3. **P5CapabilityQuery** – 能力查询
4. **P5CapabilityStateResolver** – 状态解析
5. **CapabilityVerificationScope** – 验证范围（14个字段：platform/os_version/driver_version/gpu_vendor/gpu_device_id/adapter_luid/content_profile/container_profile/codec_profile/pixel_format/decode_path/render_path/output_path/display_mode/hdr_mode/vrr_mode/test_lab_id/verification_level/evidence_id）
6. **CapabilityEvidenceLink** – 证据链接
7. **CapabilitySupportState** – 支持状态（10值：DeclaredContract/ImplementedButNotVerified/VerifiedInLab/ProductionEnabled/ExperimentalOnly/BoundaryOnly/DisabledByPolicy/Unsupported/FailClosed/Degraded）
8. **CapabilityPolicyState** – 策略状态
9. **CapabilityRuntimeState** – 运行时状态
10. **P5CapabilitySnapshot 结构体** – 含 capability_id/domain/state/support_state/verification_scope/policy_state/runtime_state/last_probe_result/evidence_id/failure_reason/user_visible_status/technical_detail_code
11. **P5CapabilityRegistry 汇总能力域** – 21个域：Demux/FFmpeg backend/Sandbox broker/Codec/Hardware decode/Software decode/D3D11VA/NV12 SRV/P010 integer plane view/P010 UNORM fallback/Zero-copy/Cross-adapter bridge/SwapChain/WaitableSwapChain/VRR/DWM timing/HDR10/HDR10+/HLG/Dolby Vision fallback/Protected playback/Output protection/Snapshot export/Subtitle attachment/Frame pacing
12. **P8 展示规则类型** – P8 只能读取 P5CapabilitySnapshot

### Allowed Operations

- 创建 `include/kivo/video_plane/capability/` 目录树
- 使用 `namespace kivo::video_plane::capability`
- 头文件为 INTERFACE 库，不 link 任何 runtime
- 所有 enum class，所有 struct 带 `operator==(...) = default`
- 文件名用小写下划线
- P5CapabilitySnapshot 包含 user_visible_status + technical_detail_code

### Forbidden Operations

- 禁止各模块各自声明"支持"（必须走 Registry）
- 禁止新增 PartiallyVerified 状态（用 VerifiedInLab + CapabilityVerificationScope 表达）
- 禁止 VerifiedInLab 自动等于 ProductionEnabled
- 禁止 Platform-specific verified 推广成 global support
- 禁止 ExperimentalOnly 自动升级 ProductionEnabled
- 禁止用户可见能力使用内部 enum 拼接
- 禁止没有 CapabilityVerificationScope 的能力写 ProductionEnabled
- 禁止头文件中 link FFmpeg/D3D11/DXGI

---

## GATE-002: Evidence Schema 统一证据记录

**Task ID**: KIVO-VIDEO-P5-GATE-002-EVIDENCE-SCHEMA
**Source Section**: §2
**Gate Stage**: P5A (contracts)
**Priority**: P0 – P5 baseline closure gate

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | P5EvidenceRecord 类型定义完成 | 含 22 个字段 |
| E2 | EvidenceSeverity 枚举（10值）完成 | Trace/Debug/Info/Warning/RecoverableError/Fatal/Security/PolicyDenied/FailClosed |
| E3 | EvidenceLossPolicy 枚举（6值）完成 | CriticalRingRequired/NonCriticalDroppable/Sampled/DebugOnly/GoldenRequired/SecurityRequired |
| E4 | P5CriticalEvidenceRing 类型定义 | Fatal/Security/PolicyDenied/FailClosed 必须进 ring |
| E5 | P5NonCriticalEvidenceDropCounter 类型 | drop 必须聚合计数 |
| E6 | P5EvidenceBackpressurePolicy 类型 | Release 构建不得因 evidence sink 不可用阻塞播放 |
| E7 | P5EvidenceSchemaMigrationNote 类型 | breaking change 必须有 migration note |

### Required Types (7)

1. **P5EvidenceRecord** – 22字段：schema_version/evidence_id/evidence_kind/severity/component_id/pipeline_id/CoreVideoStamp(optional)/FullVideoStamp(optional)/monotonic_time/media_time(optional)/thread_id(optional)/state_before(optional)/state_after(optional)/decision(optional)/reason_code/technical_detail_code/payload_ref(optional)/redaction_level/pii_safety_class/protected_content_safety_class/loss_policy/correlation_id(optional)
2. **EvidenceSeverity** – 10值 (Trace~FailClosed)
3. **EvidenceLossPolicy** – 6值
4. **P5CriticalEvidenceRing** – 环形缓冲区
5. **P5NonCriticalEvidenceDropCounter** – 原子计数
6. **P5EvidenceBackpressurePolicy** – 背压策略
7. **P5EvidenceSchemaMigrationNote** – 迁移记录

### Allowed Operations

- 所有 evidence 必须带 schema_version
- Critical evidence 不得丢
- Noncritical evidence 可丢但必须计数
- Debug 构建可 fail-fast
- P5DiagnosticsService 只读 evidence，不拥有业务状态
- Producer 不得为 reserve evidence key 阻塞热路径

### Forbidden Operations

- 禁止 evidence payload 包含 token/signed URL/cookie/DRM key/protected pixel/private full path
- 禁止 protected evidence 不 redacted
- 禁止 evidence under pressure 阻塞 render hot path
- 禁止 CriticalEvidenceRing 覆盖当前 terminal outcome 记录
- 禁止 Release 构建因 evidence sink 不可用阻塞播放

---

## GATE-003: Threat-Mitigation-Test Matrix

**Task ID**: KIVO-VIDEO-P5-GATE-003-THREAT-MITIGATION-MATRIX
**Source Section**: §3
**Gate Stage**: P5S (security, parallel from P5A)
**Priority**: P0 – P5S closure gate

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | P5ThreatMitigationMatrixItem 类型定义 | 11个字段 |
| E2 | 29个 threat surface 全部录入 matrix | 每个 threat 至少1个 mitigation |
| E3 | 每个 mitigation 至少1个 test | test name 可追溯 |
| E4 | P5S gate 检查 matrix 完整性 | 脚本 `check_p5_threat_matrix.ps1` 通过 |
| E5 | 不能只有 quarantine 作为 mitigation | 至少有两种 mitigation 方式 |

### Required Types (1)

1. **P5ThreatMitigationMatrixItem** – threat_id/threat_surface/attack_vector/affected_component/risk_level/mitigation/sandbox_required/allocation_policy/fuzz_target/regression_corpus/expected_failure_mode/evidence_kind/gate_stage

### 29 Threat Surfaces (全部必须覆盖)

malicious container / malicious codec bitstream / malicious codec extradata / malicious subtitle packet / malicious attachment / malicious font file / malicious HDR metadata / malicious Dolby metadata / timestamp overflow / duration overflow / index corruption / allocation bomb / decompression bomb / path traversal / metadata fake mime / attachment mime mismatch / live discontinuity abuse / infinite probe / demux stuck / decoder stuck / shader metadata abuse / protected content export attempt / external subtitle path abuse / debug dump leakage / sandbox IPC oversized payload / shared memory stale handle

### Allowed Operations

- 每个 threat 必须至少有一个 mitigation
- 每个 mitigation 必须至少有一个 test
- remote/untrusted content 必须 sandbox-preferred
- allocation limit/fuzz/sandbox/evidence redaction 必须能追溯到 matrix

### Forbidden Operations

- 禁止只有 quarantine 作为 mitigation
- 禁止 in-process FFmpeg 声明安全隔离
- 禁止任何 threat 没有 test

---

## GATE-004: Sandbox Broker Contract

**Task ID**: KIVO-VIDEO-P5-GATE-004-SANDBOX-BROKER-CONTRACT
**Source Section**: §4
**Gate Stage**: P5A (contracts), P5C (fake tests), P5S (closure)
**Priority**: P0 – P5S closure gate

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | 11 types defined | header files exist in `video_plane/sandbox/` |
| E2 | 13 broker messages defined | OpenStream ~ Terminate |
| E3 | IPC Message Header 含12字段 | protocol_version/request_id/pipeline_id/media_epoch/deadline/payload_size/shared_memory_handle/capability_flags/security_class/correlation_id |
| E4 | DemuxBufferPlacement 枚举（4值） | HostProcessPool/SandboxProcessPool/SharedMemoryRing/CopyBridge |
| E5 | Contract-only 阶段通过 architecture guard | 无 FFmpeg/D3D11/Qt/WASAPI 依赖 |
| E6 | Fake broker tests pass (P5S 前) | P5C gate pass |

### Required Types (11)

1. **P5SandboxBrokerContract** – 协议契约
2. **SandboxedDemuxProcess** – 沙箱 demux 进程
3. **SandboxedDecodeProcess** (optional) – 沙箱 decode 进程
4. **SandboxIpcProtocol** – IPC 协议
5. **SandboxResourceBudget** – 资源预算
6. **SandboxCrashPolicy** – 崩溃策略
7. **SandboxTimeoutPolicy** – 超时策略
8. **SandboxMemoryPolicy** – 内存策略
9. **SandboxFileAccessPolicy** – 文件访问策略
10. **SandboxSharedMemoryPolicy** – 共享内存策略
11. **SandboxBrokerEvidence** – 沙箱证据

### Broker Messages (13)

OpenStream / CloseStream / ProbeContainer / FindStreamInfo / ReadPacket / Seek / Flush / Cancel / GetTrackDescriptors / GetAttachmentDescriptor / GetOpaqueAttachmentChunk / ReportBackendEvidence / Terminate

### Allowed Operations

- Shared memory 必须有 owner + zeroing policy
- Broker crash → DemuxBackendCrashedEvidence
- Broker timeout → DeadlineExceeded / Quarantined
- Local trusted file 可走 in-process（仍受 allocation limit + fuzz corpus 约束）
- Cross-process copy count 进入 evidence
- 第一阶段可 contract-only

### Forbidden Operations

- 禁止 broker 访问 P4 provider credential
- 禁止 broker 看到 DRM key
- 禁止 broker 输出 raw private path
- 禁止 sandbox process 持有 P4 lease
- 禁止 IPC payload 无 size cap
- 禁止 crash 后 shared memory 不 quarantine/zero

---

## GATE-005: AllocationLimitPolicy

**Task ID**: KIVO-VIDEO-P5-GATE-005-ALLOCATION-LIMIT-POLICY
**Source Section**: §5
**Gate Stage**: P5A (contracts)
**Priority**: P0 – P5S closure gate

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | 12 个默认值常量定义 | 头文件中定义 |
| E2 | 超限行为 contract test | 超限不 crash，输出 bounded failure |
| E3 | Policy 可调整（trusted/untrusted/test） | 不同策略路径可测试 |

### Default Values (12)

```
MaxSingleMediaAllocationDefault        = 64 MiB
MaxSingleAttachmentDescriptorBytes     = 16 MiB
MaxSingleAttachmentBytes               = 64 MiB
MaxTotalAttachmentBytesDefault         = 256 MiB
MaxTrackCountDefault                   = 256
MaxChapterCountDefault                 = 10000
MaxMetadataEntryBytesDefault           = 1 MiB
MaxTotalMetadataBytesDefault           = 32 MiB
MaxProbeBytesDefault                   = policy-defined, capped
MaxSubtitlePacketBytesDefault          = 8 MiB
MaxSubtitleQueueBytesDefault           = 64 MiB
MaxIpcPayloadBytesDefault              = 16 MiB
```

### Allowed Operations

- 超限不 crash
- 超限输出 bounded failure
- Policy 按 trusted local / remote untrusted / test corpus 调整
- 任何提升上限必须 evidence

### Forbidden Operations

- 禁止只说 bounded 不写具体值
- 禁止默认值缺失
- 禁止 remote/untrusted 使用与 trusted 相同限额
- 禁止 protected content 因超限进入 insecure fallback

---

## GATE-006: Fuzz Scope / Fuzz Smoke / Risk Register

**Task ID**: KIVO-VIDEO-P5-GATE-006-FUZZ-SCOPE
**Source Section**: §6
**Gate Stage**: P5S
**Priority**: P0 – P5S closure gate

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | 15 fuzz targets 定义 | 每个 target 有 fuzz_target 名 |
| E2 | FuzzSmoke 结构体定义 | 7个字段 |
| E3 | P5ThirdPartyBackendRiskRegister 定义 | 10个字段 |
| E4 | Fuzz smoke minimal pass (CIGate) | zero untriaged crash |

### 15 Fuzz Targets

container demux / codec extradata / subtitle packet / attachment metadata / attachment chunk / live discontinuity / path metadata / timestamp wrap / duration overflow / allocation bomb / seek index / HDR SEI / Dolby RPU metadata / sandbox IPC payload / mime sniff conflict

### Required Types (2)

1. **FuzzSmoke** – minimum_duration_per_target / minimum_seed_corpus_count / sanitizer_config / crash_policy(zero untriaged crash) / timeout_policy / oom_policy / corpus_version / gate_stage
2. **P5ThirdPartyBackendRiskRegister** – backend_name / version / license / enabled_features / known_cves / sandbox_status / update_policy / risk_level / owner / last_review_date / distribution_status

### Allowed Operations

- "fuzz smoke pass" 必须可测
- timeout / OOM 必须有独立分类
- sanitizer 配置必须记录

### Forbidden Operations

- 禁止声称 0 crash = 安全（仅代表当前 corpus 未发现问题）
- 禁止 corpus 为空就声称 pass

---

## GATE-007: Interface Versioning / 边界兼容

**Task ID**: KIVO-VIDEO-P5-GATE-007-BOUNDARY-VERSIONING
**Source Section**: §7
**Gate Stage**: P5A (contracts)
**Priority**: P0 – P5 baseline closure gate

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | 7个版本类型定义 | P5BoundaryContractVersion / P4ToP5ContractVersion / P5ToP6ContractVersion / P5ToP7ContractVersion / P5ToP8ContractVersion / P5EvidenceSchemaVersion / P5CapabilitySchemaVersion / SandboxIpcProtocolVersion |
| E2 | Compatibility Policy 4种模式定义 | Compatible/MinorNewer/MajorMismatch/DiagnosticsOnlyMismatch |
| E3 | BoundaryVersionMismatchEvidence 类型 | version mismatch 必须输出 |
| E4 | Version contract test | P6/P7/P8 必须能拒绝不兼容版本 |

### Allowed Operations

- Breaking change → bump major
- Compatible field addition → bump minor
- Diagnostics-only addition → bump patch
- Evidence schema version 与 boundary contract version 分离
- Sandbox IPC version 与 P5 public contract version 分离

### Forbidden Operations

- 禁止隐式结构体同步跨 P4/P5/P6/P7/P8 边界
- 禁止 MajorMismatch 时接受绑定

---

## GATE-008: Pipeline Failure Arbitration

**Task ID**: KIVO-VIDEO-P5-GATE-008-FAILURE-ARBITER
**Source Section**: §8
**Gate Stage**: P5A (contracts)
**Priority**: P0 – P5 baseline closure gate

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | 5个类型定义 | PipelineFailureArbiter / PipelineFatalSignal / PipelineTerminalOutcome / PipelineFailureCorrelationId / PipelineFailureArbitrationEvidence |
| E2 | 11个 fatal signal 来源枚举 | DeviceLost unrecoverable ~ ArchitectureGuardRuntimeViolation |
| E3 | 6个 TerminalOutcome 枚举 | Recovered/RebuildStarted/SurfaceLostWaiting/FailToUpperLayer/ProcessSafetyExceeded/UserStopped |
| E4 | Single terminal outcome contract test | arbiter 输出唯一 outcome |
| E5 | RecoveryThread 测试 | 不由 producer 自己 teardown |

### Allowed Operations

- 所有 fatal signal 进入 arbiter
- 重复 fatal 被 coalesce
- Outcome 必须带 correlation_id
- RecoveryThread 执行
- P8/P4/P6/P7 通知由 arbiter 派发

### Forbidden Operations

- 禁止多个 terminal outcome
- 禁止多组件各自上报 fatal
- 禁止 producer 自己 teardown
- 禁止 RecoveryThread 多头上报
- 禁止没有 correlation_id 的 outcome

---

## GATE-009: RecoverySeek / RecoveryBookmark / StandbyUntilNewInput

**Task ID**: KIVO-VIDEO-P5-GATE-009-RECOVERY-SEEK
**Source Section**: §9
**Gate Stage**: P5A (contracts), P5E (tests)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | RecoverySeekRequest 结构体 | 8个字段，user_visible = false |
| E2 | StandbyUntilNewInput 结构体 | 6个字段 |
| E3 | 5级 live_edge_hint 优先级定义 | P4-approved > P4TimelineOriginHint > RecoveryBookmark > P5 > ConservativeLiveRebase |
| E4 | RecoverySeek 不进入 user seek history test | UI 不显示用户主动拖动 |

### Allowed Operations

- RecoverySeekRequest 不进入 user seek history
- P4 是唯一 playback position authority
- P4 可拒绝 RecoverySeekRequest
- Standby 必须有超时
- Stop 必须立即退出 Standby

### Forbidden Operations

- 禁止 Standby 期间保留大型 GPU/demux buffer
- 禁止 P4 拒绝后 P5 不走 StandbyUntilNewInput 或 FailToUpperLayer

---

## GATE-010: Queue Lifecycle / Cancellation / Starvation

**Task ID**: KIVO-VIDEO-P5-GATE-010-QUEUE-CANCELLATION
**Source Section**: §10
**Gate Stage**: P5B (tests)
**Priority**: P0 – P5 baseline closure gate

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | QueueLifecycleState 枚举（7值） | Accepting/Draining/Rejecting/Cancelled/ForcedRetired/Closed |
| E2 | CancelMode 枚举（4值） | CooperativeCancel/RejectBeforeConsume/ForceRetireIfNotStarted/HardStopForcedRetire |
| E3 | Queue item 含7字段 | queue_item_id/pipeline_id/CoreVideoStamp/cancel_token/lifecycle_state/enqueue_time/last_progress_time/correlation_id |
| E4 | Queue timeout defaults 定义（7项） | ActivePlayback=5s / LowLatencyLive=2s / Paused=disabled / WindowHidden=disabled or extended / WaitingForBuffer=policy / UserStopped=disabled / ShutdownDrain=2s |
| E5 | Idempotent cancel test | Cancel 必须 idempotent |

### Allowed Operations

- Running GPU command 不可抢占
- Running demux callback 只能 interrupt
- Protected frame cancel 必须走 protected retire order
- terminal ack/cancel 不依赖 DemuxThread

### Forbidden Operations

- 禁止 HardStop 后等待正常 consumer progress
- 禁止 terminal ack/cancel 不 idempotent
- 禁止 terminal ack/cancel 不带 correlation_id
- 禁止 P4 session revision 变化后 terminal ack/cancel 不安全

---

## GATE-011: Protected Content / BoundaryOnly / Secure Session

**Task ID**: KIVO-VIDEO-P5-GATE-011-PROTECTED-BOUNDARY
**Source Section**: §11
**Gate Stage**: P5A (contracts), P5D (lifecycle tests), P5H (closure)
**Priority**: P0 – P5 baseline closure gate

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | ProtectedContentBoundaryHint 结构体（12字段） | hint_id/source_boundary/trust_level/protected_content_id/encryption_scheme_hint/secure_decode_required/secure_output_required/output_protection_required/allow_cpu_readback/allow_snapshot/allow_thumbnail/evidence_id |
| E2 | BoundaryOnly baseline behavior test | 无 platform backend 时 deny decode/present，输出 ProtectedBoundaryOnlyFailClosedEvidence |
| E3 | ProtectedPlaceholderFrame 结构体 | size/color(black)/text(by P8)/raw pixels never/snapshot=black placeholder+reason |
| E4 | ProtectedPlaybackCapabilityState 6种映射 | BoundaryOnly→BoundaryOnly/PlatformBackend→ImplementedButNotVerified/VerifiedSecureDecode→VerifiedInLab/VerifiedSecureOutput→VerifiedInLab/ProductionEnabled→ProductionEnabled/FailClosed→FailClosed |
| E5 | SecureDecodeSessionContextHandle 结构体 + 生命周期 | 10个状态：create/bind_decoder/flush_reset/drain/release_protected_surfaces/invalidate_on_device_lost/invalidate_on_output_protection_lost/close/retire_evidence |
| E6 | 6条禁止规则全部有 test | 不 cross pipeline/serialize/use export/reuse after DeviceEpoch/reuse after OutputProtectionLost/include raw value in evidence |

### Allowed Operations

- ProtectedContentBoundaryHint 是输入 hint（不等于 license/key）
- EncryptedMediaBoundaryHint 冲突时 protected 优先
- 冲突必须 fail-closed
- BoundaryOnly 允许 black placeholder only

### Forbidden Operations

- 禁止 hint 携带 key material
- 禁止 copy handle across pipeline
- 禁止 serialize handle into evidence payload
- 禁止 use handle in snapshot/export
- 禁止 reuse handle after DeviceEpoch change
- 禁止 reuse handle after OutputProtectionLost
- 禁止 include protected handle raw value in evidence

---

## GATE-012: Multi-monitor Protected Output Mapping

**Task ID**: KIVO-VIDEO-P5-GATE-012-PROTECTED-OUTPUT-MAPPING
**Source Section**: §12
**Gate Stage**: P5F (tests)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | 4个类型定义 | SurfaceToDisplayOutputMapper / ActiveOutputSet / ProtectedOutputCoverage / ProtectedOutputStatusSnapshot |
| E2 | Window 跨多 monitor 时按最严格策略 test | fail-closed or stop protected present |
| E3 | Monitor move 刷新 mapping test | DisplayCapabilitySnapshot 过期不得声称 protected output current |

### Allowed Operations

- Protected output status 必须按实际显示输出查询
- 跨 monitor 时按最严格策略
- Monitor move 必须刷新 mapping

### Forbidden Operations

- 禁止 DisplayCapabilitySnapshot 过期时声称 protected output current
- 禁止 protected output lost on any active output 时不 fail-closed

---

## GATE-013: Dolby Vision / ICtCp / Safe Base Layer

**Task ID**: KIVO-VIDEO-P5-GATE-013-DOLBY-VISION
**Source Section**: §13
**Gate Stage**: P5G (tests), P5H (closure)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | SafeBaseLayerEvidence 结构体（11字段） | dv_profile/bl_compatibility_id/base_layer_codec/base_layer_transfer/base_layer_primaries/base_layer_matrix/enhancement_layer_ignored_policy/rpu_ignored_policy/color_path_safe/decoder_path_safe/evidence_id |
| E2 | Profile 5 FailClosed test | 输出 fail-closed evidence |
| E3 | Profile 8 fallback test | 仅 base layer HDR10/HLG compatible 证据成立时可 fallback |
| E4 | Profile 7 SafeBaseLayerEvidence test | base layer 必须有 SafeBaseLayerEvidence |
| E5 | DolbyVisionEngineGate 枚举（8步） | Authorization/Legal Review/Technical Feasibility/Prototype Behind Flag/Certification if Required/Lab Verification/Production Enablement |
| E6 | Unknown DV FailClosed test | 默认 FailClosed |

### Allowed Operations

- Experimental engine 可独立存在
- ICtCp/IPT source 有 verified conversion path 时才允许

### Forbidden Operations

- 禁止跳过 DolbyVisionEngineGate 任何一步
- 禁止 user-visible DV support claim（未完成 gate 时）
- 禁止 Experimental engine 污染 baseline ColorManagementOrchestrator
- 禁止 ICtCp/IPT 没有 verified conversion path 时不 FailClosed
- 禁止 RPU ignored 没有 explicit evidence
- 禁止 Enhancement layer ignored 没有 user-visible warning 或 technical evidence

---

## GATE-014: HDR10+ / FailClosed / Fallback

**Task ID**: KIVO-VIDEO-P5-GATE-014-HDR10PLUS
**Source Section**: §14
**Gate Stage**: P5G (tests), P5H (closure)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | Hdr10PlusCapability 6种状态进入 P5CapabilityRegistry | DetectedOnly/OpaquePreserved/ParsedButNotApplied/DynamicToneMapUnsupported/AppliedExperimental/AppliedVerified |
| E2 | 6种 → CapabilitySupportState 映射 test | DetectedOnly→DeclaredContract/ImplementedButNotVerified 等 |
| E3 | Hdr10PlusOpaqueMetadataPolicy 结构体（6字段） | max_payload_size/schema_validation/redaction_policy/evidence_storage_policy/export_allowed/display_output_allowed |
| E4 | FallbackNotSafe / FailClosedIfUnsafe 语义 test | FallbackNotSafe 可继续寻找；FailClosedIfUnsafe 是 terminal safe outcome |

### Allowed Operations

- Opaque preserve 不等于 dynamic tone mapping 支持
- Metadata payload 必须 size capped
- Malformed dynamic metadata 必须 fail or downgrade with evidence

### Forbidden Operations

- 禁止 evidence 保存巨大 raw metadata
- 禁止 export 不 obey redaction policy
- 禁止 protected content / DV Profile 5 / unsafe output protection 不走 FailClosedIfUnsafe

---

## GATE-015: P010 / Plane SRV / Numeric Tolerance

**Task ID**: KIVO-VIDEO-P5-GATE-015-P010-FORMULA
**Source Section**: §15
**Gate Stage**: P5G (tests), P5H (closure)
**Priority**: P0（P5 Gate Review 前必须存在）

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | P010 limited range 无歧义公式定义（3个公式） | Y_limited/Cb_limited/Cr_limited 使用 clamp 写法 |
| E2 | P010 code recovery 精确路径定义 | R16_UINT 路径：code10 = stored_word >> 6；UNORM fallback：code10 = round(sampled_unorm16 * 65535.0 / 64.0) |
| E3 | P010UnormRecoveryTolerance 3项容差定义 | code10_tolerance ≤1 / normalized_luma ≤1/876 / normalized_chroma ≤1/448 |
| E4 | P010PlaneAccessEvidence 结构体（9字段） | format_support_result/srv_create_result/view_format/shader_path/golden_result/driver_vendor/driver_version/device_id/adapter_luid |
| E5 | Golden numeric test | 对比 reference vs 实现 |

### Allowed Operations

- 使用精确整数路径
- UNORM fallback 必须有 tolerance 记录

### Forbidden Operations

- ❌ 禁止写成：`(code - 512) / (960 - 64) * 2`（歧义公式）
- ❌ 禁止错误公式：`code10 = round(sampled_unorm16 * 1023.0 / 65535.0)`
- ❌ 禁止没有 IntegerPlaneViewVerified Evidence 就声称 P010 支持

---

## GATE-016: D3D11VA / FFmpeg hwcontext / COM Ownership

**Task ID**: KIVO-VIDEO-P5-GATE-016-D3D11VA-CONTEXT
**Source Section**: §16
**Gate Stage**: P5G (tests), P5H (closure)
**Priority**: P0（P5 Gate Review 前必须存在）

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | Mode A (LockBridge) 与 Mode B (Dedicated) 二选一 | 选 Type A 或 Type B，不能都不选 |
| E2 | D3D11TextureOpaqueRef 结构体（8字段） | resource_id/com_ref_policy/array_slice/subresource_index/device_epoch/adapter_luid/release_owner=GpuDeviceThread |
| E3 | D3D11DecodeRenderSyncMode 枚举（6值） | SameDeviceImmediateContextSerialized/SameDeviceCopyQueueSerialized/SharedTextureKeyedMutexExperimental/CpuCopybackDisallowedForProtected/Unsupported |
| E4 | 420_OPAQUE fallback test | 5种允许 fallback；no direct shader/no av_hwframe_transfer_data assumed |

### Allowed Operations

- AddRefOwned 由 release_owner 释放
- COM Release 只能在 GpuDeviceThread 或 maintenance context

### Forbidden Operations

- ❌ 禁止口头证明 FFmpeg 不跨线程
- ❌ 禁止多线程无序调用同一个 immediate context
- ❌ 禁止 DecodeThread 直接调用 render immediate context
- ❌ 禁止 RenderThread 直接调用 decode video context
- ❌ 禁止 BorrowedWithinLease 跨 lease
- ❌ 禁止 UI thread Release GPU resource
- ❌ 禁止持有 P5 lock 时 COM Release
- ❌ 禁止 RenderThread hot path 触发未知 COM destructor

---

## GATE-017: DXGI SwapChain / BackBuffer / Live Object

**Task ID**: KIVO-VIDEO-P5-GATE-017-DXGI-SWAPCHAIN
**Source Section**: §17
**Gate Stage**: P5F (tests), P5H (closure)
**Priority**: P0（P5 Gate Review 前必须存在）

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | 6个类型定义 | BackBufferReferenceTracker/BackBufferViewRegistry/DeferredCommandListResourceTracker/SwapChainResizePreflightCheck/D3D11LiveObjectTracker/SwapChainBindingReleaseEvidence |
| E2 | Resize/Rebuild 前置规则 test（7条） | 所有 backbuffer view 创建必须注册 / wrapper 记录 parent backbuffer generation / deferred command list 无法追踪时 discard 等 |
| E3 | Flush timeout 测试 | DefaultResizeFlushTimeout=100ms/DebugResizeFlushTimeout=1000ms/Timeout→SurfaceRebuildFailedEvidence |
| E4 | GDI DC 规则 test | 仅 GDI-compatible swapchain 需 release DC |

### Allowed Operations

- All backbuffer view 创建必须注册
- All wrapper 必须记录 parent backbuffer generation
- Debug 必须 live-object report
- Release 不无限 retry

### Forbidden Operations

- 禁止 ResizeBuffers 前不 release RTV/SRV/DSV/backbuffer wrapper
- 禁止持有 lock 时等待
- 禁止未启用 GDI-compatible 时执行虚假 release DC 路径

---

## GATE-018: VRR / Waitable SwapChain / Rational Cadence

**Task ID**: KIVO-VIDEO-P5-GATE-018-VRR-SWAPCHAIN
**Source Section**: §18
**Gate Stage**: P5F (tests), P5H (closure)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | VrrState 枚举（9值） | NotInitialized/Unsupported/Eligible/Attempted/ActiveTelemetryConfirmed/FallbackToFixedRefresh/DisabledByPolicy/Failed |
| E2 | ActiveTelemetryConfirmed 3种允许来源 | trusted platform present telemetry / OS API reports VRR active / lab measurement |
| E3 | ActiveTelemetryConfirmed 3种禁止来源 test | tearing flag alone / monitor marketing name alone / refresh-rate range alone |
| E4 | WaitableSwapChainState 枚举（8值） | NotCreated/Unsupported/CreatedWithFlag/WaitHandleAcquired/WaitHandleFailed/WaitingBeforeRender/TimeoutDegraded/Closed |
| E5 | Rational cadence accumulator bound test | overflow → reset with evidence / no silent wrap / no float-only cadence decision |

### Allowed Operations

- 仅 trusted platform present telemetry/OS API/lab measurement 可产生 ActiveTelemetryConfirmed
- Accumulator overflow → reset with evidence
- All math checked

### Forbidden Operations

- 禁止 tearing flag alone 产生 ActiveTelemetryConfirmed
- 禁止 monitor marketing name alone 产生 ActiveTelemetryConfirmed
- 禁止 refresh-rate range alone 产生 ActiveTelemetryConfirmed
- 禁止 silent wrap
- 禁止 float-only cadence decision

---

## GATE-019: Color / HDR / Chroma / Geometry

**Task ID**: KIVO-VIDEO-P5-GATE-019-COLOR-HDR-CHROMA
**Source Section**: §19
**Gate Stage**: P5H (closure)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | ColorMetadataAuthorityPolicy 4条规则 test | bitstream vs container/user override/conservative default |
| E2 | ChromaSitingPlan 结构体（5字段）+ 4 location 定义 | Left/Center/TopLeft/Unknown，各自 offset |
| E3 | 4种 siting golden test | MPEG2 Left / JPEG Center / TopLeft / Unknown conservative default |
| E4 | 5种 Color golden numeric diff 阈值 test | YUV ≤1 code / matrix ≤1e-4 / 8-bit SDR ≤1 LSB / HDR tone-map monotonicity / FusedShaderEquivalenceRecord |
| E5 | HDR transition fade policy test | display-referred linear-light domain |
| E6 | DefaultSdrPaperWhiteNits = 203 | user configurable |
| E7 | FrameExportColorPolicy 枚举（4值） | SourceReferred/DisplayReferred/SdrMapped/DebugRawNonProtectedOnly |
| E8 | Tone-map monotonicity proof test | reference implementation / dense sampled / edge-case / golden curve diff |

### Allowed Operations

- Bitstream codec-derived colorimetry 优先于 container
- Shader coordinate offset 由 ChromaSitingPlan 生成
- Fade 在 display-referred linear-light domain

### Forbidden Operations

- 禁止所有 4:2:0 默认 Center（必须区分 MPEG2 Left vs JPEG Center）
- 禁止 tone-map monotonicity 只用 visual approval
- 禁止 tone-map monotonicity 只用 metadata propagation
- 禁止 user override 当作 metadata truth（只能 explicit override）

---

## GATE-020: Timebase / Checked Rational / Live Timestamp

**Task ID**: KIVO-VIDEO-P5-GATE-020-TIMEBASE
**Source Section**: §20
**Gate Stage**: P5E (tests)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | checked_rescale 三级路径 test | try int64 → else int128 → else fail TimestampOverflow |
| E2 | LiveTimestampOriginResolver 7级优先级 test | P4TimelineOriginHint→segment absolute time→PCR/PTS→first good PTS→last good live edge→ConservativeLiveRebase→TimestampInvalid |
| E3 | LowLatencyLive reorder policy test | reorder_delay ≤ budget → normal / ≤ max → catch-up / else → skip to live edge or fail |
| E4 | LiveCatchupPolicy 结构体（4字段） | target_latency_ms/max_latency_ms/drop_threshold_ms/skip_to_live_edge_threshold_ms |
| E5 | LowLatencyLive heavy path policy test | may lower quality but must not wrong-color / must not bypass fail-closed |

### Allowed Operations

- int64 safe path 优先
- overflow 作为显式错误输出
- LowLatencyLive 可降低 tone-map 质量但不可 wrong-color

### Forbidden Operations

- ❌ 禁止 silent wrap（checked_rescale 必须显式 overflow）
- ❌ 禁止 double-only timestamp conversion
- ❌ 禁止 unchecked multiply
- ❌ 禁止 bypass fail-closed

---

## GATE-021: Attachment Safety / Subtitle Packet Safety

**Task ID**: KIVO-VIDEO-P5-GATE-021-ATTACHMENT-SAFETY
**Source Section**: §21
**Gate Stage**: P5B (attachment tests), P5S (security)
**Priority**: P0 – P5S closure gate

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | MIME conflict policy test（3种情况） | declared≠sniffed: unsafe→reject / font→non-font→reject / unknown→AcceptedDescriptorOnly or reject |
| E2 | OpaqueAttachmentFlowHandle 结构体（5字段+2方法） | attachment_id/map_id/byte_size/safety_state + read_chunk()/close() |
| E3 | 3种 QuotaScope 定义 | PerContainer/PerPipeline/PerSubtitleTrack/PerAttachmentType |
| E4 | Default quota values | MaxAttachmentsPerContainer=256 / MaxTotalAttachmentBytes=256MiB / MaxSingleAttachmentBytes=64MiB |
| E5 | SubtitlePacketSafetyPolicy 结构体（5字段） | max_subtitle_packet_size/max_subtitle_queue_bytes/malformed_packet_policy/handoff_deadline/p7_backpressure_policy |
| E6 | External subtitle boundary types（7个） | ExternalSubtitleBoundaryHint/ExternalSubtitlePathPolicy/SubtitleEncodingDetectionPolicy/ExternalSubtitlePrivatePathRedaction/ExternalSubtitleSizeCap/ExternalSubtitleFuzzCorpus |

### Allowed Operations

- P7 不拿 private path
- Chunk size capped
- Read timeout-bound
- Content hash optional
- Protected attachment policy separate

### Forbidden Operations

- 禁止 P7 拿 private path
- 禁止 chunk size 无上限
- 禁止 declaration mime ≠ sniffed mime 时不 emit AttachmentMimeMismatchEvidence
- 禁止 external subtitle private path 进入 evidence
- 禁止 untrusted external subtitle 不应用 fuzz/safety policy

---

## GATE-022: PresentationEpoch / Geometry / Hold Last Frame

**Task ID**: KIVO-VIDEO-P5-GATE-022-PRESENTATION-EPOCH
**Source Section**: §22
**Gate Stage**: P5F (tests)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | Subtitle-only change 语义 test（6条排除） | viewport/crop/rotation/surface/output color/protected output change → not subtitle-only |
| E2 | HoldLastFrameSafe 条件 test（6个条件） | same protected output status/old output encoding valid/old surface valid/old frame not protected-invalid/not stale DeviceEpoch/color transition policy allows |
| E3 | Geometry pipeline 顺序锁定 test（7步） | coded frame→crop→chroma siting→SAR→rotation→scale mode→viewport→output transform |
| E4 | GeometryMetadataAuthorityPolicy test | verified bitstream display matrix > container display metadata > user override > conservative fallback |

### Allowed Operations

- Pipeline 顺序锁死
- HoldLastFrameSafe 所有条件满足才 hold

### Forbidden Operations

- 禁止 subtitle-only change 时改变 P5 presentation geometry
- 禁止 HoldLastFrameSafe 条件不满足时还 hold（应 fade-to-black or placeholder）

---

## GATE-023: P5 -> P6 / P7 / P8 Output Boundary

**Task ID**: KIVO-VIDEO-P5-GATE-023-OUTPUT-BOUNDARY
**Source Section**: §23
**Gate Stage**: P5A (contracts)
**Priority**: P0（P5 Gate Review 前必须存在）

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | AudioPacketBoundaryItem 结构体（13字段） | track_id/packet_id/pts/dts/duration/timebase/codec_id/stream_revision/media_epoch/discontinuity_flags/data_handle/size_hint/keyframe_flag |
| E2 | SubtitleCompositionTargetSnapshot 3级confidence定义 | High/Medium/Low + 各自 degrade 行为 |
| E3 | RepeatReason 枚举（6值） | CadenceRepeat/PauseHold/BufferUnderrunHold/TransitionHold/WindowHiddenNoPresent/GpuWouldBlockReuse |
| E4 | last_estimated_visible_at 5级来源定义 | PresentTelemetry > DwmTiming > RenderScheduler > FixedPolicyEstimate > Unavailable |

### Allowed Operations

- P5 不 decode audio
- P5 不决定 passthrough
- P6 owns decode/output/clock
- Audio packet flow 必须有 flush/discontinuity 语义

### Forbidden Operations

- 禁止 P5 decode audio
- 禁止 P6 backpressure 反控 P4
- 禁止 last_estimated_visible_at 不带 confidence

---

## GATE-024: GPU Budget / Device Lost / Display Freshness

**Task ID**: KIVO-VIDEO-P5-GATE-024-GPU-BUDGET
**Source Section**: §24
**Gate Stage**: P5G (tests)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | 4个类型定义 | GpuBudgetProvider/GpuBudgetSnapshot/BudgetPressureLevel/GpuBudgetEvidence |
| E2 | 8种 GPU 资源种类区分 test | decoder surfaces/render targets/staging textures/backbuffers/HDR intermediates/export readbacks/quarantined resources/shared texture bridge resources |
| E3 | DeviceRemovedReason 5种原因记录 | DXGI_ERROR_DEVICE_REMOVED/DEVICE_RESET/DEVICE_HUNG/DRIVER_INTERNAL_ERROR/Unknown |
| E4 | DisplayCapabilitySnapshot 结构体 + 过期规则 test | 过期时 hold last safe frame / request DisplayThread refresh / 不 claim HDR/VRR/output protection current |

### Allowed Operations

- Device removed 原因映射 recovery action
- DisplayCapabilitySnapshot 过期时停止声称能力

### Forbidden Operations

- 禁止过期后还 claim HDR current
- 禁止过期后还 claim VRR current
- 禁止过期后还 claim output protection current

---

## GATE-025: Thread Runtime Enforcement / COM Release

**Task ID**: KIVO-VIDEO-P5-GATE-025-THREAD-RUNTIME
**Source Section**: §25
**Gate Stage**: P5A (contracts), P5D (tests)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | 5个类型定义 | ThreadAffinityToken/ThreadContractRuntimeChecker/LockRankRuntimeChecker/ForbiddenBlockingCallDetector/ComReleasePolicy |
| E2 | Debug fail-fast test | 违反 thread affinity → fail-fast |
| E3 | Release poison pipeline test | 违反时 poison |
| E4 | No callback into P4/P8/P6/P7 while holding P5 lock test | 持有锁时不回调外部 |
| E5 | COM release rules test（8条） | only GpuDeviceThread / not on UI thread / not on RenderThread hot path 等 |

### Allowed Operations

- Debug fail-fast
- Release poison pipeline
- COM release only on GpuDeviceThread or maintenance context

### Forbidden Operations

- 禁止持有锁时 evidence sink blocking
- 禁止持有锁时 COM release with unknown callbacks
- 禁止持有 P5 lock 时 callback into P4/P8/P6/P7
- 禁止 UI thread COM Release
- 禁止 RenderThread hot path 触发 COM destructor

---

## GATE-026: Architecture Guard Target Graph

**Task ID**: KIVO-VIDEO-P5-GATE-026-ARCHITECTURE-GUARD
**Source Section**: §26
**Gate Stage**: P5A (initial), P5H (closure)
**Priority**: P0（P5 Gate Review 前必须存在）

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | 7个 CMake targets 定义 | kivo_video_plane_contracts/core/ffmpeg_demux/ffmpeg_decode/d3d11_gpu/dxgi_swapchain/tests/future_backend_contracts |
| E2 | 10条 link 规则 test | contracts 不 link FFmpeg/D3D11/Qt/WASAPI 等 |
| E3 | 4个 symbol checker 定义 | PublicExportSymbolScan/InternalSymbolScan/ForbiddenAbiTypeScan/DependencyGraphScan/BinaryExportSurfaceScan |
| E4 | Windows binary scan test | dumpbin /exports + CMake link graph |

### Allowed Operations

- contracts 不 link 任何 runtime
- ffmpeg backend 可 link FFmpeg
- d3d11 backend 可 link D3D11/DXGI
- future backend 不进入 default include path
- future backend 不进入 baseline binary

### Forbidden Operations

- 禁止 public headers 出现 raw third-party types
- 禁止 binary export 暴露 forbidden symbols
- 禁止 future backend 出现在 public facade
- 禁止 future backend 影响 P5A~P5H build graph
- 禁止 core 暴露 FFmpeg/D3D11 public ABI

---

## GATE-027: License / Patent / Distribution

**Task ID**: KIVO-VIDEO-P5-GATE-027-LICENSE-PATENT
**Source Section**: §27
**Gate Stage**: P5C (risk register), P5H (closure)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | 5个类型定义 | CodecPatentRiskReport/RegionalDistributionPolicy/BinaryRedistributionChecklist/SourceRelinkCompliancePackage/ThirdPartyNoticeBundle |
| E2 | LGPL dynamic linking 默认 | GPL/nonfree 默认禁止 |
| E3 | FFmpeg risk register (from GATE-006) | P5ThirdPartyBackendRiskRegister |

### Allowed Operations

- LGPL dynamic linking 默认
- Patent 风险进入分布风险报告

### Forbidden Operations

- 禁止 GPL/nonfree 默认允许
- 禁止 commercial release 无 binary redistribution checklist

---

## GATE-028: Telemetry Privacy / Diagnostics Retention

**Task ID**: KIVO-VIDEO-P5-GATE-028-TELEMETRY-PRIVACY
**Source Section**: §28
**Gate Stage**: P5H (closure)
**Priority**: P0（P5 Gate Review 前必须存在）

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | TelemetryPrivacyPolicy 结构体（7字段） | device_id_hashing_policy/path_redaction_policy/opt_in_out_policy/crash_upload_policy/local_only_mode/retention_days |
| E2 | DiagnosticsRetentionPolicy 结构体（6字段） | retention_days/max_local_storage/crash_bundle_policy/user_consent_required/deletion_policy/protected_content_redaction_policy |
| E3 | 5条规则 test | 不保存 raw file path/device id hash or redacted/protected content redacted/crash upload 用户同意/diagnostics 清理 bounded |

### Allowed Operations

- Device id hash or redacted
- Protected content redacted
- 用户 consent 控制 crash upload

### Forbidden Operations

- 禁止 telemetry 保存 raw file path
- 禁止 device id 未 hash/redacted
- 禁止 protected content evidence 未 redacted
- 禁止 diagnostics 清理 unbounded

---

## GATE-029: Formal State Monitor

**Task ID**: KIVO-VIDEO-P5-GATE-029-FORMAL-STATE-MONITOR
**Source Section**: §29
**Gate Stage**: P5A (contracts), P5H (closure)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | FormalStateMonitor 9个输入源定义 | queue events/epoch changes/thread events/recovery transitions/swapchain transitions/protected transitions/failure arbiter outcomes/sandbox broker transitions/output protection transitions/backbuffer reference transitions |
| E2 | FormalStateViolation 3种分类 test | ProgrammingFormalViolation(Debug fail-fast)/RuntimeFormalViolation(Release poison)/DataTriggeredFormalViolation(fail closed) |
| E3 | Formal violation 进入 FailureArbiter test | 所有 violation → FailureArbiter |

### Allowed Operations

- ProgrammingFormalViolation → Debug fail-fast
- RuntimeFormalViolation → Release poison pipeline or rebuild
- DataTriggeredFormalViolation → fail closed / bounded failure

### Forbidden Operations

- 禁止 Formal violation 不进入 FailureArbiter
- 禁止 ProgrammingFormalViolation 在 Debug 时 silent

---

## GATE-030: Test / CI / Hardware Lab 分层

**Task ID**: KIVO-VIDEO-P5-GATE-030-TEST-CI-LAB
**Source Section**: §30
**Gate Stage**: P5A (initial definition), ongoing
**Priority**: P0 (infrastructure)

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | 4种 Gate 类型定义 | CIGate/NightlyStressGate/HardwareLabGate/ReleaseCandidateGate |
| E2 | CIGate 内容定义 | contract tests/fake backend behavior/architecture guard/static checker/golden numeric synthetic/sandbox fake broker/fuzz smoke minimal |
| E3 | NightlyStressGate 内容定义 | longer fuzz/stress queue/starvation watchdog/quarantine stress/device lost fake stress/soak smoke |
| E4 | HardwareLabGate 内容定义 | real D3D11VA/real NV12 P010/real HDR monitor/real VRR/real waitable swapchain/real monitor move/real output protection |
| E5 | ReleaseCandidateGate 内容定义 | full compatibility matrix/license bundle/patent report/telemetry privacy review/diagnostics retention review/soak stress/golden update approval |
| E6 | D3D11 Debug Layer 规则 test | Lab gate 使用/Release 不依赖/absence ≠ pass/absence 记录 evidence |

### Allowed Operations

- Lab gate 使用 D3D11 debug layer
- Release 不依赖 debug layer
- Debug layer absence → record evidence

### Forbidden Operations

- 禁止把 Debug layer absence 当作 pass
- 禁止 Release 依赖 D3D11 debug layer

---

## GATE-031: Malformed Corpus Governance

**Task ID**: KIVO-VIDEO-P5-GATE-031-MALFORMED-CORPUS
**Source Section**: §31
**Gate Stage**: P5S
**Priority**: P0 – P5S closure gate

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | MalformedCorpusItem 结构体（9字段） | corpus_id/source/risk_class/expected_behavior/owner/added_by/added_at |
| E2 | 删除规则 test | reason/replacement/reviewer approval/before after coverage impact |
| E3 | 禁止为通过测试删除 malformed corpus test | 删除必须有 reviewer |

### Allowed Operations

- 删除必须有 reason + replacement + reviewer approval
- Golden 更新影响 capability 输出时触发 capability evidence revalidation

### Forbidden Operations

- 禁止为通过测试删除 malformed corpus
- 禁止 corpus 删除无 reviewer

---

## STAGE PLAN: P5A ~ P5H / P5S 阶段 Gate 概览

**Source Section**: §32

### P5A - Contract Definition (Exit Criteria: 15 items)

| # | Criterion | Gate |
|---|-----------|------|
| A1  | public contracts compile | ALL |
| A2  | CapabilityRegistry contract complete | GATE-001 |
| A3  | EvidenceSchema contract complete | GATE-002 |
| A4  | CoreStamp / FullStamp contract complete | (GATE-002) |
| A5  | P5StampValidator contract complete | (GATE-002) |
| A6  | QueueLifecycleState contract complete | GATE-010 |
| A7  | Threading matrix compile-time docs | GATE-025 |
| A8  | P4 recovery seek boundary defined | GATE-009 |
| A9  | Bootstrap single-owner rule defined | (new) |
| A10 | PresentationEpoch trigger table draft | GATE-022 |
| A11 | Security threat model skeleton | GATE-003 |
| A12 | Architecture graph guard skeleton runnable | GATE-026 |
| A13 | No FFmpeg/D3D11/Qt/WASAPI in public contract target | GATE-026 |
| A14 | Developer guide skeleton | GATE-034 |
| A15 | BoundaryContractVersion types defined | GATE-007 |
| A16 | FailureArbiter contract defined | GATE-008 |

### P5B - P4 Input Bridge (Exit Criteria: 10 items)

| # | Criterion |
|---|-----------|
| B1 | P4VideoInputBridge fake tests pass |
| B2 | EncodedInputIOAdapter fake tests pass |
| B3 | DemuxBufferPool allocation cap tests pass |
| B4 | Queue lifecycle tests pass |
| B5 | OutOfBandBackpressure atomic publish tests pass |
| B6 | AttachmentSafetyDescriptor tests pass |
| B7 | FakeDemuxBackend demuxes known corpus |
| B8 | Unknown bitrate fallback evidence emitted |
| B9 | No P4 lease retained beyond allowed window |
| B10 | FFmpeg copy-to-pool default contract exists |
| B11 | ProgressWatchdog contract tests pass |

### P5C - FFmpeg Demux Backend (Exit Criteria: 11 items)

| # | Criterion |
|---|-----------|
| C1 | FFmpeg wrapper isolated in ffmpeg/** only |
| C2 | AVIO bridge copy-to-pool default verified |
| C3 | Interrupt callback bound before open/probe/find/read |
| C4 | find_stream_info deadline test pass |
| C5 | Demux quarantine test pass |
| C6 | Zombie quarantine test pass |
| C7 | FFmpeg risk register complete |
| C8 | License manifest generated |
| C9 | Malformed demux corpus smoke pass |
| C10 | Sandbox broker contract fake tests pass |
| C11 | Track discovery tests pass |
| C12 | Subtitle attachment map tests pass |

### P5D - Decoder Contract (Exit Criteria: 9 items)

| # | Criterion |
|---|-----------|
| D1 | Decoder contract complete |
| D2 | Fake decoder produces frames |
| D3 | FrameLease / RetireToken tests pass |
| D4 | FramePool recycle tests pass |
| D5 | No heavy destructor on RenderThread test pass |
| D6 | Queue drain escape hatch test pass |
| D7 | SecureDecodeSessionContext BoundaryOnly lifecycle tests pass |
| D8 | Protected export denied tests pass |
| D9 | COM release policy tests pass |

### P5E - Software Decode / Timing (Exit Criteria: 10 items)

| # | Criterion |
|---|-----------|
| E1 | Software decode path works on baseline corpus |
| E2 | Checked rational rescale tests pass |
| E3 | Timestamp wrap tests pass |
| E4 | VFR timing tests pass |
| E5 | B-frame reorder tests pass |
| E6 | Seek warmup discard tests pass |
| E7 | Fallback rewind to keyframe tests pass |
| E8 | RecoveryBookmark and RecoverySeekRequest tests pass |
| E9 | Live timestamp origin resolver tests pass |
| E10 | Low-latency reorder budget tests pass |

### P5F - GPU Rendering (Exit Criteria: 13 items)

| # | Criterion |
|---|-----------|
| F1 | D3D11 device boundary tests pass |
| F2 | Fake render path works |
| F3 | DXGI swapchain contract tests pass |
| F4 | BackBuffer direct/indirect ref release tests pass |
| F5 | Waitable swapchain lifecycle tests pass |
| F6 | DWM latency hint evidence emitted |
| F7 | VRR Eligible/Attempted/ActiveTelemetryConfirmed distinction tests pass |
| F8 | Rational cadence golden tests pass |
| F9 | Rotation 90/270 viewport recompute tests pass |
| F10 | Device removed simulation tests pass |
| F11 | PresentationEpoch trigger/non-trigger tests pass |
| F12 | Display capability freshness tests pass |

### P5G - D3D11VA / P010 / GPU (Exit Criteria: 11 items)

| # | Criterion |
|---|-----------|
| G1 | D3D11VA hwcontext bridge tests pass |
| G2 | Array texture / slice resolver tests pass |
| G3 | D3D11 context lock bridge tests pass |
| G4 | NV12 plane SRV tests pass |
| G5 | P010 IntegerPlaneViewVerified gate tests pass |
| G6 | P010 UnormPlaneViewVerified gate tests pass |
| G7 | P010 golden numeric tests pass |
| G8 | SRV creation failure fallback tests pass |
| G9 | Adapter topology tests pass |
| G10 | Cross-adapter production ban tests pass |
| G11 | Shared texture experimental gate remains off |
| G12 | Tier A real hardware evidence or safe-fail evidence |
| G13 | GPU budget provider tests pass |

### P5H - Color/HDR/Protected Closure (Exit Criteria: 18 items)

| # | Criterion |
|---|-----------|
| H1 | ColorMetadataAuthority tests pass |
| H2 | ColorMetadataConflict tests pass |
| H3 | ChromaSitingPlan golden tests pass |
| H4 | ShaderExecutionOrder tests pass |
| H5 | HDR10 static fallback tests pass |
| H6 | HDR10+ Detected/OpaquePreserved/DynamicToneMapUnsupported tests pass |
| H7 | HLG SDR fallback tests pass |
| H8 | DV Profile 5 fail-closed tests pass |
| H9 | DV user message tests pass |
| H10 | Protected BoundaryOnly closure tests pass |
| H11 | OutputProtection lost fail-closed tests pass |
| H12 | Formal hard state tables complete |
| H13 | Soak tests pass |
| H14 | Performance baseline report |
| H15 | Developer guide complete |
| H16 | Telemetry privacy policy reviewed |
| H17 | Diagnostics retention policy reviewed |

### P5S - Security Closure (runs parallel from P5A, blocks P5H closure)

| # | Criterion |
|---|-----------|
| S1 | ThreatMitigationMatrix complete |
| S2 | Sandbox broker protocol reviewed |
| S3 | Allocation policy tests pass |
| S4 | Attachment quota tests pass |
| S5 | Metadata sanitizer tests pass |
| S6 | Malformed corpus smoke pass |
| S7 | Fuzz smoke pass |
| S8 | Security evidence redaction tests pass |
| S9 | Third-party backend risk register complete |
| S10 | CVE response process documented |
| S11 | Telemetry privacy policy complete |
| S12 | Diagnostics retention policy complete |
| S13 | Debug dump security policy complete |

---

## GATE-032: Skeleton / Draft / Complete DoD

**Task ID**: KIVO-VIDEO-P5-GATE-032-DOD
**Source Section**: §33
**Priority**: P0 (applied to all types)

### DoD Levels

**Skeleton**: public types named / ownership noted / failure state noted / at least one contract test placeholder

**Draft**: fields defined / lifecycle defined / state transitions listed / minimum tests listed

**Complete**: tests implemented / guard passes / evidence schema linked / gate reviewer approved

---

## GATE-033: Developer Guide

**Task ID**: KIVO-VIDEO-P5-GATE-033-DEVELOPER-GUIDE
**Source Section**: §34
**Gate Stage**: P5A (skeleton), P5H (complete)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | File `docs/developer/p5_video_plane_developer_guide.md` | 包含 11 个场景 |
| E2 | 每个场景含 8 项内容 | pipeline flow/key types/thread flow/stamp changes/capability states/error path/test names/what P5 owns/what P5 does not own |

### 11 Required Scenarios

1. 本地 MP4 SDR 播放
2. HEVC HDR10 / P010 播放
3. 在线 live low-latency stream
4. Protected content / output protection loss
5. Dolby Vision Profile 5 fail-closed
6. Malformed file bounded failure
7. Device lost during HDR playback
8. Monitor move SDR -> HDR
9. D3D11VA P010 fallback
10. Live low latency catch-up
11. Subtitle attachment safety

---

## GATE-034: Frame Export / Debug Dump Security

**Task ID**: KIVO-VIDEO-P5-GATE-034-FRAME-EXPORT
**Source Section**: §35
**Gate Stage**: P5S
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | FrameExportSecurityPolicy 结构体（5条规则） | DebugDumpDisabledByDefault/ProtectedDebugDumpForbidden/RedactionRequired/PrivatePathForbidden/UserConsentRequiredForCrashBundle |
| E2 | 5 snapshot/thumbnail golden tests | SDR source→SDR snapshot/HDR source→SDR mapped/HDR source→display-referred/P010 source→correct range/rotation crop applied/protected placeholder |

### Allowed Operations

- Debug dump 默认关闭
- User consent required for crash bundle

### Forbidden Operations

- 禁止 protected debug dump
- 禁止 snapshot 含 private path
- 禁止 protected content 出现在 debug dump

---

## GATE-035: Enhancement Plugin Security

**Task ID**: KIVO-VIDEO-P5-GATE-035-PLUGIN-SECURITY
**Source Section**: §36
**Gate Stage**: P5S (future, 默认关闭)
**Priority**: P1 (future gate)

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | EnhancementPluginSecurityPolicy 结构体（6字段） | plugin_sandbox_policy/plugin_gpu_resource_access_policy/plugin_timeout_policy/plugin_crash_isolation/plugin_abi_compatibility/plugin_signature_trust_policy |

### Allowed Operations

- Plugin 默认关闭（不允许在 P5 baseline 默认开启）
- Plugin timeout 必须 fallback

### Forbidden Operations

- 禁止 plugin 访问 provider credential
- 禁止 plugin 访问 DRM key
- 禁止 plugin 拿 raw protected pixels
- 禁止 plugin crash crash P5
- 禁止 production plugin 没有 signature/trust policy

---

## GATE-036: Window Minimized / Occluded Resource Policy

**Task ID**: KIVO-VIDEO-P5-GATE-036-WINDOW-VISIBILITY
**Source Section**: §37
**Gate Stage**: P5F (tests)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | WindowVisibilityVideoPolicy 结构体（4字段 + 4行为） | visible_policy/occluded_policy/minimized_policy/hidden_policy |
| E2 | 4种 visibility 行为 test | Visible: normal / Occluded: may reduce present / Minimized: no present / Hidden: no present + no expensive HDR shader |

### Allowed Operations

- Occluded: may reduce present, may continue decode
- Minimized: no present, may pause render queue, must keep timeline policy explicit
- Hidden: no present, keep minimal resources

### Forbidden Operations

- 禁止 Minimized/Hidden 时继续 expensive HDR shader
- 禁止 Hidden 时保留大型 GPU 资源

---

## GATE-037: Success Definition 可测化

**Task ID**: KIVO-VIDEO-P5-GATE-037-SUCCESS-DEFINITION
**Source Section**: §38
**Gate Stage**: P5A (initial), P5H (final check)
**Priority**: P0

### Evidence Target

| # | Evidence | Pass Condition |
|---|----------|---------------|
| E1 | SuccessDefinitionItem 结构体（8字段） | item_id/statement/test_name/gate_stage/evidence_kind/required_environment/pass_threshold/failure_outcome |
| E2 | 所有成功定义有对应 test name | 不包括口号式声明 |

### Allowed Operations

- 每个 success statement → 绑定 test_name
- 每个 success → 绑定 evidence_kind

### Forbidden Operations

- 禁止 "security-conscious, evidence-backed" 这类口号作为 acceptance criterion
- 禁止 宣传口径放 Acceptance Criteria 区

---

## GATE-038: P5 Change Control

**Task ID**: KIVO-VIDEO-P5-GATE-038-CHANGE-CONTROL
**Source Section**: §40
**Gate Stage**: P5A onwards
**Priority**: P0 (process)

### P5ChangeControl Structure

```
P5ChangeControl {
    delta_id
    affected_sections
    compatibility_impact
    gate_impact
    security_impact
    capability_impact
    evidence_schema_impact
    owner
    reviewer
    approval_status
    rollback_plan
}
```

### Rules

1. breaking boundary change → must bump version
2. security gate change → must P5S reviewer approve
3. capability state change → must update P5CapabilityRegistry
4. evidence schema change → must migration note
5. golden output change → must trigger capability revalidation

---

## FINAL GATE CHECKLIST (P5 Baseline Closure)

**Source Section**: §39

### P0 Gates (must exist before P5 Gate Review)

| # | Gate | Description |
|---|------|-------------|
| 1 | P5CapabilityRegistry | 统一能力注册 |
| 2 | P5EvidenceRecord schema | 统一证据 |
| 3 | P5ThreatMitigationMatrix | 安全威胁闭环 |
| 4 | P5SandboxBrokerContract | 沙箱协议 |
| 5 | P5A~P5H/P5S Exit Criteria | 阶段定义 |
| 6 | P010 unambiguous formula + tolerance | 数值精确 |
| 7 | D3D11VA lock/context ownership | GPU 安全 |
| 8 | DXGI BackBufferReferenceTracker | 资源追踪 |
| 9 | PipelineFailureArbiter | 统一裁决 |
| 10 | Queue cancellation semantics | 队列语义 |
| 11 | Protected BoundaryOnly behavior | 安全边界 |
| 12 | DV safe base layer predicate | DV 安全 |
| 13 | HDR10+ opaque preserve policy | HDR10+ 安全 |
| 14 | ColorMetadataConflictPolicy | 颜色权威 |
| 15 | ChromaSitingPlan | 色度定位 |
| 16 | CheckedMediaTimeRescale | 时间安全 |
| 17 | P5→P6 AudioPacketBoundaryItem | 输出边界 |
| 18 | Architecture Guard target graph | 架构门 |
| 19 | Boundary contract versioning | 版本化 |
| 20 | Telemetry privacy / diagnostics retention | 隐私安全 |

### P5 Baseline Closure Gates (all must pass)

CapabilityRegistry Gate / EvidenceSchema Gate / ThreatMitigationMatrix Gate / SandboxBrokerContract Gate / BoundaryVersioning Gate / PipelineFailureArbiter Gate / QueueCancellation Gate / QueueStarvationWatchdog Gate / AllocationLimitPolicy Gate / FuzzSmoke Gate / ThirdPartyBackendRiskRegister Gate / ProtectedBoundaryOnly Gate / SecureDecodeSessionContext Gate / ProtectedOutputMapping Gate / DVSafeBaseLayer Gate / DolbyVisionEngineGatePolicy Gate / HDR10PlusOpaquePreserve Gate / P010FormulaTolerance Gate / D3D11VAContextLock Gate / D3D11OpaqueTextureOwnership Gate / DXGIBackBufferReferenceTracker Gate / VRRTelemetryState Gate / WaitableSwapChainLifecycle Gate / ColorMetadataAuthority Gate / ChromaSitingPlan Gate / CheckedRationalTimebase Gate / AttachmentSafety Gate / OpaqueAttachmentFlow Gate / P5ToP6AudioPacketBoundary Gate / ArchitectureTargetGraph Gate / BinarySymbolChecker Gate / TelemetryPrivacy Gate / DiagnosticsRetention Gate / FormalStateMonitorInput Gate / DeveloperGuideExpanded Gate / StageExitCriteria Gate / SuccessDefinitionMeasurable Gate

---

## GLOBAL PROHIBITIONS (跨所有 Gate)

### 跨边界禁止

| # | 禁止项 |
|---|--------|
| F1 | 禁止 P5 声明拥有 source/provider selection |
| F2 | 禁止 P5 声明拥有 playback command control |
| F3 | 禁止 P5 声明拥有 audio output/decode/passthrough |
| F4 | 禁止 P5 声明拥有 subtitle rendering/shaping |
| F5 | 禁止 P5 声明拥有 UI |
| F6 | 禁止 P5 声明拥有 platform media session |
| F7 | 禁止 P5 声明拥有 DRM license acquisition/key handling |
| F8 | 禁止 P5 声明拥有 HLS/DASH playlist decision |
| F9 | 禁止 P5 声明拥有 provider retry/token |
| F10 | 禁止 P5 声明拥有 user-facing playback policy arbitration outside boundary |

### 实现禁止

| # | 禁止项 |
|---|--------|
| F11 | 禁止 P5A Planning Draft 前任何实现 |
| F12 | 禁止 P5 Gate Review 前 P5A 代码进入 master |
| F13 | 禁止跳过 V2.2 合并规则 |
| F14 | 禁止用"口号式能力声明"替代可测试验收项 |

---

## VERIFICATION: V2.2 Final Judgment (Section 41)

P5 Video Media Plane owns:
- demux, video decode, frame lifecycle, GPU resource lifecycle
- adapter topology handling, swapchain/render surface
- frame pacing, VRR-aware pacing, live video pacing
- color management orchestration, shader execution planning
- color pipeline, HDR metadata, HDR10+ metadata boundary
- HLG fallback, Dolby Vision fallback classification
- protected content surface boundary, secure decode session context boundary
- protected stop order, output protection awareness
- tone mapping policy, video preroll readiness
- video frame presentation evidence, DWM composition latency hint
- frame export boundary, video output boundaries
- capability reporting, evidence schema, threat mitigation mapping
- sandbox broker boundary, architecture graph guard
- measurable closure gates

P5 does NOT own:
- source/provider selection, playback command control
- audio output/decode/passthrough decision
- subtitle rendering/shaping, UI, platform media session
- DRM license acquisition, DRM key handling
- HLS/DASH playlist decision, provider retry/token
- user-facing playback policy arbitration outside its boundary

---

**End of P5 V2.2 Execution Card**
**Total Gates: 38 | Total Stage Exit Criteria: ~120**
**Next: P5A Planning Draft → Gate Review → Implementation**
