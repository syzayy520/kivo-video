# KIVO-VIDEO-P8-SUBTITLE-PLANE-V1.5.5 EXECUTION CARD

**Source**: `C:\Users\huyasi\Downloads\Kivo Video P8 Subtitle Plane Design V1.5.5.docx`
**Source SHA-256**: `9AC07D575468533312CB403CED5CD368372F2DC248CC5232F3B492B8F72B4197`
**Task ID**: `KIVO-VIDEO-P8-SUBTITLE-PLANE-DESIGN-LOCK-001-V1.5.5`
**Document type**: execution-card / planning-only
**Generated**: 2026-06-29
**Status**: `P8_EXECUTION_CARD_CREATED_IMPLEMENTATION_NOT_STARTED`
**Current allowed write in this task**: this execution card only.

## 0. Scope Lock

P8 is **Subtitle Plane Core**.

P8 owns the subtitle plane core contract and runtime boundary: pure C public ABI, API bootstrap/version negotiation, C ABI scalar and public type closure, subtitle source descriptors/readers, discovery priority, track and embedded subtitle stream contracts, plane state/timeline model, command lifecycle, frame leases, snapshot leases, snapshot blob binary layout, evidence/metrics, privacy redaction/hash policy, cancellation polling, observer callbacks, ASS/SSA/WebVTT semantic closure, ABI layout guards, contract tooling, and deterministic fake validation.

P8 does **not** own UI, OCR, online subtitle search, AI subtitle runtime, renderer layout, text shaping, P9 player shell, Qt adapter, FFmpeg demux ownership, Win32 renderer, D3D/DXGI renderer contracts, provider API, playlist/next-episode orchestration, or rewrites of frozen P4/P5/P6/P7 public contracts.

P8 must not expose C++ ABI, Qt, FFmpeg, Win32 renderer, STL containers, C++ exceptions, bare `noexcept`, or implementation-only dependency types through the public C ABI.

## 0.1 Workspace Lock

`C:\kivo video` is the **only** working directory for P8.

Allowed:

- Read the user-provided P8 source DOCX at `C:\Users\huyasi\Downloads\Kivo Video P8 Subtitle Plane Design V1.5.5.docx` as an explicit read-only design input.
- Read and write approved P8 files inside `C:\kivo video`.
- Write P8 evidence only under `artifacts/p8/evidence/`.
- Write P8 roadmap, closure, design-manifest, tool, source, and test files only under the semantic family paths named by the active gate.

Forbidden:

- New clone, new worktree, mirror checkout, external backup directory, or hidden implementation staging outside `C:\kivo video`.
- Writing extracted DOCX text, generated P8 code, temporary implementation, evidence, or reports outside `C:\kivo video`.
- Treating the Downloads DOCX as mutable project state.
- Root-level P8 scratch files, root-level generated reports, or flat dumping-ground directories.

Any accidental need for outside workspace writes is a STOP condition and requires explicit user approval before continuing.

## 0.2 Natural Family / No-Omission / Single-Responsibility Lock

Before any implementation stage starts, the stage must have a natural family tree with:

- intended directories,
- intended file names,
- one-sentence responsibility for each file,
- owner gate,
- allowed dependencies,
- forbidden dependencies,
- evidence target,
- mapped exit criteria.

No stage may start with an incomplete family tree. No implementation file may mix unrelated responsibilities. No broad `common`, `misc`, `utils`, `helpers`, `shared`, or root-level dumping-ground file is allowed.

No design requirement may be silently dropped. Each stage closure must include a no-omission matrix mapping V1.5.5 design requirements, V1.5.4/V1.5.3/V1.5.2 carryover requirements when available, and S1-S30 exit criteria to evidence.

## 0.3 Proposed Natural Family Tree

The following tree is a planning baseline. P8A must verify and may refine it before P8B implementation starts.

```text
include/kivo/video/subtitle_plane/
  kivo_subtitle_plane.h                         public pure C ABI entrypoint and all C ABI public types

backend/include_private/video/subtitle_plane/
  abi/                                          ABI validation, size/offset expectations, API table builders
  command/                                      command token, lifecycle, conflict-free query internals
  core/                                         plane owner, state machine, timeline state, runtime caps
  discovery/                                    candidate discovery, priority ranking, track selection internals
  evidence/                                     evidence ring, summary, metrics, privacy-safe diagnostics
  format/                                       SRT/SSA/ASS/WebVTT model and semantic normalization internals
  frame/                                        frame model, frame lease, frame pool, hot path policy
  generation/                                   source/track/parser/timeline/frame generation helpers
  metrics/                                      metrics counters, snapshots, and overflow accounting
  observer/                                     observer registry and callback delivery guard
  privacy/                                      keyed hash, redacted identity, local diagnostic policy
  snapshot/                                     snapshot store, snapshot lease, snapshot blob serializer
  source/                                       source descriptor validation, external reader adapter, stream end
  text/                                         text buffer and font blob store internals
  threading/                                    cancellation polling, hazard retry, close/tombstone policy

backend/src/video/subtitle_plane/
  abi/                                          non-header ABI bootstrap implementation
  command/                                      command lifecycle implementation
  core/                                         plane lifecycle and timeline implementation
  discovery/                                    discovery priority implementation
  evidence/                                     evidence and metrics implementation
  format/                                       subtitle parser semantic helpers
  frame/                                        frame lease and pool implementation
  metrics/                                      metrics snapshot implementation
  observer/                                     observer implementation
  privacy/                                      hash/redaction implementation
  snapshot/                                     snapshot lease/blob implementation
  source/                                       source reader and embedded packet implementation
  text/                                         text/font blob implementation
  threading/                                    cancellation and hazard/tombstone implementation

backend/tests/video/subtitle_plane/
  abi/                                          pure C header, bootstrap, ABI layout, forbidden-token tests
  command/                                      command token and lifecycle tests
  discovery/                                    discovery priority and source ranking tests
  format/                                       SSA/ASS/WebVTT semantic tests
  frame/                                        frame lease and hot path tests
  observer/                                     observer callback and subscription tests
  snapshot/                                     snapshot lease/blob/endian tests
  source/                                       source reader, stream end, cancellation race tests
  test_support/                                 deterministic fake sources, fake hash keys, fake clocks

backend/tools/video/subtitle_plane/
  abi_layout_guard/                             size/offset/static-assert generation and report tooling
  architecture_guard/                           P8 family boundary and forbidden dependency guard
  c_header_purist/                              C header token scanner and C compiler probe tooling
  contract_gap_scanner/                         design/test/implementation gap scanner
  matrix_codegen/                               S1-S30 and requirement matrix generator

docs/design/p8/
  KIVO-VIDEO-P8-SUBTITLE-PLANE-DESIGN-LOCK-001-V1.5.5.md
  KIVO-VIDEO-P8-SUBTITLE-PLANE-DESIGN-LOCK-001-V1.5.5.rule_manifest.yaml

docs/roadmap/
  p8_v1.5.5_execution_card.md
  p8a_planning_draft.md
  p8*_closure_report.md
  p8_exit_criteria_s1_s30_matrix.md

artifacts/p8/evidence/
  P8*.json
```

## 1. Global Iron Gate Rules

1. Conception before planning.
2. Planning design before natural family tree.
3. Natural family tree before implementation.
4. Implementation only after the current stage approval gate.
5. One file, one clear responsibility.
6. No flat file dumping. New files must live under a semantic family tree.
7. Evidence before claims. No PASS without real command output, test output, git diff, or evidence file.
8. Any `FAIL`, unclassified hard gap, or evidence/card mismatch stops the current stage.
9. Existing user or prior-agent changes must not be reverted.
10. P8 implementation must not modify P4/P5/P6/P7 frozen public contracts unless a gate explicitly records a compatibility gap and the user approves the contract change.
11. `C:\kivo video` is the only project working directory.
12. Natural family tree, no-omission matrix, and single-file single-responsibility are mandatory gates, not style preferences.
13. P8 cannot be delivered until P8A through P8H are closed with evidence.
14. Final reports must distinguish `CONTRACT_PASS`, `RUNTIME_PASS`, `BLOCKED_ENV`, `SCHEMA_DEGRADED`, `NOT_IMPLEMENTED`, and `FAIL`; static evidence cannot be described as runtime proof.

## 2. Evidence Vocabulary

Use the project evidence language:

- `RUNTIME_PASS`: runtime behavior verified by command/test/evidence.
- `CONTRACT_PASS`: contract/static/compile-only gate verified.
- `FAIL`: gate ran and failed.
- `BLOCKED_ENV`: required local environment, hardware, SDK, compiler, or dependency missing.
- `NOT_IMPLEMENTED`: not built yet.
- `SKIPPED_TEST_ONLY`: intentionally skipped test-only branch with explanation.
- `SCHEMA_DEGRADED`: validator/tooling unsupported; never report PASS in this case.

Every evidence JSON must include:

- `task_id`
- `gate_id`
- `stage`
- `status`
- `source_design`
- `commands`
- `artifacts`
- `allowed_files`
- `forbidden_files_checked`
- `exit_criteria_covered`
- `not_verified_boundaries`
- `stop_condition`

Evidence root:

```text
artifacts/p8/evidence/
```

## 3. Stage Ordering

```text
P8A -> P8B -> P8C -> P8D -> P8E -> P8F -> P8G -> P8H
```

No later stage starts until the previous stage has a closure report and an explicit next-stage authorization. P8A may create planning documents and evidence only. P8B is the first stage allowed to create P8 public contract headers, and only after `P8A-007 IMPLEMENTATION_AUTHORIZATION_GATE`.

## 4. Stage Matrix

| Stage | Gate range | Purpose | Creates implementation files |
|---|---:|---|---|
| P8A | P8A-001..007 | Preflight, source hash, carryover inventory, dependency boundary, natural family, no-omission matrix, authorization | No |
| P8B | P8B-001..009 | Public pure C ABI header, all public type closure, function table declaration, C header purist gate | Yes, public C contract only |
| P8C | P8C-001..007 | Bootstrap implementation, API table population, command lifecycle, timeline/revision/caps/cancellation core | Yes |
| P8D | P8D-001..008 | Source reader, redacted source identity, discovery priority, track/format, embedded packet stream end | Yes |
| P8E | P8E-001..009 | Parser semantics, timeline availability, generation freshness, frame lease, hot path, EOS/hazard behavior | Yes |
| P8F | P8F-001..008 | Snapshot lease/blob, evidence/metrics, observer callbacks, text/font access, privacy evidence, tombstone close | Yes |
| P8G | P8G-001..008 | Contract tooling, architecture guard, ABI layout guard, deterministic harness, fuzz/race/property tests | Yes, tooling/tests only |
| P8H | P8H-001..005 | Accumulated validation, redline scan, S1-S30 closure matrix, final report, commit readiness | No new product surface |

## 5. Exit Criteria Coverage

| Criteria | Covered by |
|---|---|
| S1 C header compiles as pure C | P8B-008, P8G-002, P8H-001 |
| S2 No bare noexcept in C ABI header | P8B-001, P8B-008, P8G-002 |
| S3 No std:: namespace in C ABI header | P8B-001, P8B-008, P8G-002 |
| S4 Bootstrap returns KivoP8ApiStatus | P8B-002, P8C-002, P8G-004 |
| S5 KIVO_P8_API_V1_REQUIRED_PREFIX_SIZE defined | P8B-002, P8C-002, P8G-004 |
| S6 Function table begins with KivoP8AbiHeader | P8B-007, P8C-003, P8G-004 |
| S7 All V1 required function pointers non-null | P8B-007, P8C-003, P8G-004 |
| S8 KivoMutableByteSpan defined | P8B-005 |
| S9 KivoSubtitleInputBytesView naming unified | P8B-005, P8G-002 |
| S10 All referenced public C types defined | P8B-003..007, P8G-001 |
| S11 FrameLease/SnapshotLease fully defined | P8B-006, P8E-007, P8F-001 |
| S12 ObserverCallbacks fully defined | P8B-006, P8F-005 |
| S13 StreamEndRequest fully defined | P8B-006, P8D-006 |
| S14 Source vtable versioned with KivoP8AbiHeader | P8B-006, P8D-001 |
| S15 ResourceAccessToken irrelevant-field rule unambiguous | P8B-006, P8D-002 |
| S16 Discovery external preference flag exists | P8B-006, P8D-004 |
| S17 Snapshot section table complete | P8B-006, P8F-002 |
| S18 Metrics/string/text snapshot policy explicit | P8B-006, P8F-002, P8F-007 |
| S19 Privacy hash/redacted identity closed | P8B-006, P8D-003, P8F-008 |
| S20 Cancellation callback replaces volatile pointer | P8B-006, P8C-007, P8G-002 |
| S21 UINT64_MAX cap behavior explicit | P8C-006 |
| S22 Cold-start late packet behavior explicit | P8E-003 |
| S23 EOS epsilon source explicit | P8E-009 |
| S24 Hazard retry bound explicit | P8E-009 |
| S25 ABI layout static assert gate complete | P8B-009, P8G-003 |
| S26 Snapshot endian responsibility explicit | P8F-003 |
| S27 Close lease tombstone policy complete | P8F-006 |
| S28 SSA/ASS/WebVTT missing semantic sections complete | P8E-004..006 |
| S29 All V1.5.5 tests PASS | P8G-004..008, P8H-001 |
| S30 No P4/P5/P6/P7 frozen public contract modification | P8A-004, P8G-007, P8H-002 |

## 6. Design Delta Coverage

| Design item | Covered by |
|---|---|
| C-001 API required prefix size | P8B-002, P8C-002, P8G-004 |
| C-002 no direct noexcept in C ABI header | P8B-001, P8G-002 |
| C-003 callback no direct noexcept | P8B-006, P8G-002 |
| C-004 KivoMutableByteSpan | P8B-005 |
| C-005 KivoSubtitleInputBytesView only | P8B-005, P8G-002 |
| G-001 external source vtable has KivoP8AbiHeader | P8B-006, P8D-001 |
| G-002 resource token irrelevant-field rules | P8D-002 |
| G-003 discovery external preference priority | P8D-004 |
| G-004 snapshot blob referenced types | P8B-006, P8F-002 |
| G-005 KivoSubtitleFrameLease | P8B-006, P8E-007 |
| G-006 CommandToken/CommandId relation | P8B-004, P8C-004 |
| G-007 KivoSubtitleSubscriptionToken | P8B-006, P8F-005 |
| G-008 KivoSubtitleObserverCallbacks | P8B-006, P8F-005 |
| G-009 P8 playback/graph revision mirrors | P8B-003, P8C-005 |
| G-010 redacted identity/hash/key policy | P8D-003, P8F-008 |
| G-011 KivoSubtitleSeekEpoch | P8B-003, P8E-003 |
| G-012 SSA/ASS/WebVTT semantics | P8E-004..006 |
| G-013 TextBufferId/FontBlobId | P8B-003, P8F-007 |
| G-014 request revision equals state revision | P8C-005 |
| G-015 KivoEmbeddedSubtitleStreamEndRequest | P8B-006, P8D-006 |
| A-001 UINT64_MAX cap rejected | P8C-006 |
| A-002 cold-start late packet behavior | P8E-003 |
| A-003 EOS hold epsilon source | P8E-009 |
| A-004 hazard retry upper bound | P8E-009 |
| A-005 source_identity_hash source | P8D-003 |
| I-001 ABI layout static assert gate | P8B-009, P8G-003 |
| I-002 snapshot blob endian responsibility | P8F-003 |
| I-003 close/frame/snapshot tombstone | P8F-006 |

## 7. P8A - Preflight / Planning / Approval

### P8A-001: Workspace and Dirty State Inventory

**Goal**: Establish the real starting point before planning or implementation.

**Evidence target**:
- `artifacts/p8/evidence/P8A-001.json`
- `git -c safe.directory='C:/kivo video' status --short`
- current branch, initial HEAD, remote HEAD if available, and full dirty/untracked inventory
- list of pre-existing files that must not be reverted or overwritten

**Allowed**:
- Read repo state.
- Use read-only git commands.
- Record dirty files without editing them.

**Forbidden**:
- `git reset`, `git clean`, checkout/revert of user files, force push.
- Creating worktrees or clones.
- Starting P8 implementation.

**Design reference**: sections 0, 29.

### P8A-002: P8 V1.5.5 Source Document Extraction and Hash

**Goal**: Lock the exact design source used for P8 planning.

**Evidence target**:
- `artifacts/p8/evidence/P8A-002.json`
- SHA-256 for `C:\Users\huyasi\Downloads\Kivo Video P8 Subtitle Plane Design V1.5.5.docx`
- extracted paragraph/heading count
- extraction method and tool version
- source status `FINAL_DESIGN_BASELINE_CANDIDATE_FOR_P8A_IMPLEMENTATION_PLANNING`

**Allowed**:
- Read the DOCX.
- Produce read-only extraction evidence.
- Write evidence only under `artifacts/p8/evidence/`.

**Forbidden**:
- Editing the DOCX.
- Replacing V1.5.5 with older V1.5.4/V1.5.3/V1.5.2 unless explicitly required as carryover evidence.
- Claiming V1.5.5 is implemented.

**Design reference**: sections 0, 1, 27, 29.

### P8A-003: V1.5.4 / V1.5.3 / V1.5.2 Carryover Baseline Gate

**Goal**: Resolve the design statement that all non-conflicting earlier tests remain required.

**Evidence target**:
- `artifacts/p8/evidence/P8A-003.json`
- existence/hash check for any available P8 V1.5.4, V1.5.3, and V1.5.2 sources or manifests
- carryover matrix of earlier tests and requirements
- conflict-resolution notes where V1.5.5 supersedes older wording
- `BLOCKED_ENV` or explicit replacement decision if earlier sources are not available

**Allowed**:
- Search project docs, `.codex`, `.codebuddy`, and user-provided source locations for earlier P8 baselines.
- Build a carryover matrix from verified earlier sources.
- If earlier sources are missing, mark the carryover unknowns clearly and stop before implementation unless the user approves a replacement decision.

**Forbidden**:
- Fabricating V1.5.4/V1.5.3/V1.5.2 content.
- Treating V1.5.5 as a strict complete superset without evidence.
- Dropping earlier required tests silently.

**Design reference**: sections 0, 26, 27.

### P8A-004: Frozen Contract Boundary Inventory

**Goal**: Reverify the P4/P5/P6/P7 surfaces that P8 must integrate with without modifying frozen public contracts.

**Evidence target**:
- `artifacts/p8/evidence/P8A-004.json`
- inventory of existing:
  - `include/kivo/video/playback_graph/**`
  - `include/kivo/video/audio_plane/**`
  - `include/kivo/video_plane/**`
  - `backend/include/kivo/cinema_engine/subtitle_core/**`
  - existing P7 tools/tests/evidence relevant to integration
- list of forbidden frozen files
- list of P8 adapter-only touch points, if any

**Allowed**:
- Read P4/P5/P6/P7 public headers and existing subtitle stubs.
- Classify needed adapters as P8-owned or external contract gaps.
- Record contract gaps.

**Forbidden**:
- Editing P4/P5/P6/P7 frozen public headers.
- Renaming or moving existing P7/P6/P5/P4 files.
- Assuming P7 revision types can be reused without an explicit P8 mirror or adapter rule.

**Design reference**: sections 0, 5, 26, 28.

### P8A-005: Natural Family Tree and File Responsibility Manifest

**Goal**: Freeze the P8 natural family tree before implementation.

**Evidence target**:
- `artifacts/p8/evidence/P8A-005.json`
- `docs/roadmap/p8a_planning_draft.md`
- proposed file list, one-sentence responsibility, owner gate, allowed dependencies, forbidden dependencies, and evidence target per file
- proof that no root-level or dumping-ground P8 files are planned

**Allowed**:
- Create planning docs only.
- Refine the proposed tree from section 0.3.
- Define public/private/test/tool/report ownership boundaries.

**Forbidden**:
- Creating implementation files.
- Adding broad `common`, `utils`, `helpers`, `shared`, or root-level P8 buckets.
- Starting CMake product target edits before the family tree is approved.

**Design reference**: sections 0, 23, 29, 30.

### P8A-006: No-Omission and Gap Registry Gate

**Goal**: Ensure every V1.5.5 item and every verified carryover item has a planned gate.

**Evidence target**:
- `artifacts/p8/evidence/P8A-006.json`
- `docs/roadmap/p8_exit_criteria_s1_s30_matrix.md`
- requirement-to-gate matrix for C-001..C-005, G-001..G-015, A-001..A-005, I-001..I-003, S1-S30, and verified earlier carryover tests
- contract gap report with severity and owner

**Allowed**:
- Generate and review planning matrices.
- Mark unresolved gaps as STOP conditions.
- Propose user decisions only where evidence cannot resolve the requirement.

**Forbidden**:
- Proceeding with any unmapped design requirement.
- Hiding carryover gaps under implementation convenience.
- Marking `CONTRACT_PASS` or `RUNTIME_PASS` from planning-only artifacts.

**Design reference**: sections 1, 27, 28, 30.

### P8A-007: Implementation Authorization Gate

**Goal**: Explicitly authorize P8B after P8A planning is complete.

**Evidence target**:
- `artifacts/p8/evidence/P8A-007.json`
- P8A closure report
- final approved natural family tree
- final P8B allowlist
- explicit statement: `P8B_ALLOWED` or `P8B_BLOCKED`

**Allowed**:
- Close P8A.
- Ask for or record explicit authorization to start P8B.

**Forbidden**:
- Starting P8B files before this gate is closed.
- Treating this execution card alone as implementation authorization.

**Design reference**: sections 29, 30.

## 8. P8B - Public Pure C ABI Contract

### P8B-001: C Header Shell, Includes, Export, Calling Convention, Noexcept Macro

**Goal**: Create the public C ABI header shell that compiles as C and never leaks C++ tokens.

**Evidence target**:
- `artifacts/p8/evidence/P8B-001.json`
- `include/kivo/video/subtitle_plane/kivo_subtitle_plane.h`
- scan proving only `<stdint.h>` and `<stddef.h>` are included by the public C ABI header
- scan proving no bare `noexcept`, `std::`, `class`, Qt, FFmpeg, Win32 renderer, STL, or C++ exception token leaks

**Allowed**:
- Create the public C ABI header under the approved family path.
- Define `KIVO_P8_API`, `KIVO_P8_CALL`, and `KIVO_P8_NOEXCEPT`.
- Use `extern "C"` guarded by `#ifdef __cplusplus`.

**Forbidden**:
- `.hpp` for the public C ABI entrypoint.
- Including C++ standard library headers in the C ABI header.
- Exposing Qt, FFmpeg, Win32 renderer, Direct3D, DXGI, or STL types.

**Design reference**: section 2.

### P8B-002: ABI Version, Status, Struct Header, and Bootstrap Declaration

**Goal**: Define version constants, status enums, `KivoP8AbiHeader`, required prefix constant, and bootstrap declaration.

**Evidence target**:
- `artifacts/p8/evidence/P8B-002.json`
- `KIVO_P8_ABI_MAJOR == 1`
- `KIVO_P8_ABI_MINOR == 5`
- `KIVO_P8_ABI_PATCH == 5`
- `KIVO_P8_ABI_REVISION == 155`
- `KIVO_P8_API_V1_REQUIRED_PREFIX_SIZE` defined
- `kivo_subtitle_plane_get_api_v1` returns `KivoP8ApiStatus`

**Allowed**:
- Define public C constants, status enums, and versioned struct header.
- Declare bootstrap function only; implementation belongs to P8C.

**Forbidden**:
- Returning `bool` from bootstrap.
- Using `size_t` in ABI fields where design requires `uint64_t`.
- Defining compiler-specific packing.

**Design reference**: sections 3, 4.

### P8B-003: Core Scalar IDs, Revisions, Epochs, and Generation Types

**Goal**: Define all scalar wrappers and generation structs referenced by the public ABI.

**Evidence target**:
- `artifacts/p8/evidence/P8B-003.json`
- definitions for `KivoSubtitleTime`, `KivoSubtitleDelayOffset`, `KivoSubtitleSeekEpoch`, `KivoP8PlaybackRevision`, `KivoP8GraphSnapshotRevision`
- definitions for all command, correlation, track, cue, frame, snapshot, source, packet, text, font, string, evidence, subscription, discovery, and lease IDs
- definitions for content, packet, timeline, and frame generation structs

**Allowed**:
- Define opaque numeric wrappers exactly as C ABI types.
- Document value `0` invalid where required.

**Forbidden**:
- Reusing P7/P6 C++ types directly in the C ABI.
- Hiding undefined public referenced types for later.

**Design reference**: sections 5, 8.

### P8B-004: Error, Result, Command Token, and Command Lifecycle Public Types

**Goal**: Define all public command and error types needed before runtime implementation.

**Evidence target**:
- `artifacts/p8/evidence/P8B-004.json`
- `KivoSubtitleResultStatus`
- `KivoSubtitlePlaneError`
- `KivoSubtitleCommandToken`
- `KivoSubtitleCommandLifecycleState`
- `KivoSubtitleCommandLifecycleSnapshot`
- command token relation test plan: `command_id.value == 0` means rejected before allocation

**Allowed**:
- Define public enums and structs.
- Keep command implementation out of P8B.

**Forbidden**:
- Returning raw exceptions or C++ error wrappers through the ABI.
- Leaving `ContractGap` as the only implemented command error.

**Design reference**: section 6.

### P8B-005: String, Input Bytes, and Mutable Byte Span Closure

**Goal**: Close all public byte and string input naming requirements.

**Evidence target**:
- `artifacts/p8/evidence/P8B-005.json`
- `KivoSubtitleInputStringView`
- `KivoSubtitleInputBytesView`
- `KivoMutableByteSpan`
- forbidden-token scan proving `KivoInputBytesView` does not appear in P8 public ABI

**Allowed**:
- Define call-lifetime view semantics in public comments.
- Use `uint64_t bytes`, not `size_t`.

**Forbidden**:
- Any public `KivoInputBytesView` token.
- Treating strings as NUL-terminated unless bytes include NUL.
- Persisting caller memory without documented copy semantics.

**Design reference**: section 7.

### P8B-006: Public Type Closure for Source, Privacy, Discovery, Frame, Snapshot, Observer, Cancellation, Stream End

**Goal**: Define every referenced public C type before implementation.

**Evidence target**:
- `artifacts/p8/evidence/P8B-006.json`
- public definitions for source descriptor, source vtable, privacy policy, hash key provider, redacted source identity, format/track/embedded codec, plane state, timeline availability, frame lease, snapshot lease, snapshot blob header/section, evidence summary, metrics snapshot, resource token, discovery flags, cancellation view, observer callbacks, subscription token, and embedded stream end request
- all top-level versioned structs begin with `KivoP8AbiHeader` where required

**Allowed**:
- Define public structs/enums exactly needed by V1.
- Use forward declaration only where the type is intentionally opaque and not layout-required.

**Forbidden**:
- Leaving a referenced public type undefined.
- Callback function pointers without `KIVO_P8_NOEXCEPT`.
- `volatile uint32_t*` cancellation contract.

**Design reference**: sections 9..20.

### P8B-007: Function Table V1 Declaration

**Goal**: Declare the complete V1 function table and required non-null fields.

**Evidence target**:
- `artifacts/p8/evidence/P8B-007.json`
- `KivoSubtitlePlaneApiV1` begins with `KivoP8AbiHeader`
- all required V1 function pointer fields listed
- optional future fields absent or explicitly reserved at tail
- prefix size calculation source recorded

**Allowed**:
- Declare function pointer types and table layout.
- Reserve tail fields only when design permits.

**Forbidden**:
- Bare `noexcept`.
- `bool` returns in ABI function pointers where status is required.
- Function pointer fields that are silently nullable in V1.

**Design reference**: section 26.

### P8B-008: C Header Purist Compile and Forbidden Token Gate

**Goal**: Prove the public header compiles as C and stays pure.

**Evidence target**:
- `artifacts/p8/evidence/P8B-008.json`
- C compiler probe source and command
- no-bare-noexcept scan
- no-`std::` scan
- forbidden token scan for Qt, FFmpeg, Win32 renderer, STL containers, C++ ABI, and `KivoInputBytesView`

**Allowed**:
- Add compile-only test files under `backend/tests/video/subtitle_plane/abi/`.
- Use MSVC C mode and clang-cl C mode when available.
- Mark `BLOCKED_ENV` only if the required compiler is genuinely unavailable.

**Forbidden**:
- Passing by compiling as C++ only.
- Ignoring scan hits as cosmetic.
- Reporting PASS when the validator is missing; use `SCHEMA_DEGRADED` or `BLOCKED_ENV`.

**Design reference**: sections 2, 23, 27.

### P8B-009: Initial ABI Layout Manifest Gate

**Goal**: Record size, alignment, and field offsets for all public C ABI structs.

**Evidence target**:
- `artifacts/p8/evidence/P8B-009.json`
- generated or hand-audited layout manifest for every public C ABI struct
- list of top-level structs with `KivoP8AbiHeader` at offset 0
- documented small-wrapper exceptions

**Allowed**:
- Add ABI layout compile tests under `backend/tests/video/subtitle_plane/abi/`.
- Add tooling skeleton under `backend/tools/video/subtitle_plane/abi_layout_guard/`.

**Forbidden**:
- Compiler-specific packing.
- Omitting any public struct from the layout manifest.
- Claiming cross-compiler layout match without MSVC/clang-cl evidence or a clear `BLOCKED_ENV`.

**Design reference**: section 23.

## 9. P8C - Bootstrap / API Table / Command / Timeline Core

### P8C-001: CMake Target and Runtime Skeleton Gate

**Goal**: Introduce P8 build integration without disturbing existing targets.

**Evidence target**:
- `artifacts/p8/evidence/P8C-001.json`
- changed CMake files
- list of new P8 source/test files
- proof existing P7/P6/P5/P4 files were not rewritten

**Allowed**:
- Add P8 source files under `backend/src/video/subtitle_plane/**`.
- Add P8 tests under `backend/tests/video/subtitle_plane/**`.
- Add only the minimal CMake entries required for P8 tests/runtime.

**Forbidden**:
- Broad CMake refactors unrelated to P8.
- Removing or renaming existing tests.
- Changing frozen public contract directories.

**Design reference**: sections 0, 26, 28.

### P8C-002: Bootstrap Behavior Implementation

**Goal**: Implement deterministic `kivo_subtitle_plane_get_api_v1`.

**Evidence target**:
- `artifacts/p8/evidence/P8C-002.json`
- tests for null `out_api`, too-small prefix, unsupported revision, unsupported endian, and valid table population
- `KivoP8ApiStatus` result assertions

**Allowed**:
- Implement API table population under `backend/src/video/subtitle_plane/abi/`.
- Zero output on failure if required by implementation contract.

**Forbidden**:
- Returning `bool`.
- Partially initialized success tables.
- Accepting too-small prefix by accident.

**Design reference**: section 4.

### P8C-003: Function Table Non-Null Gate

**Goal**: Prove all V1 required function pointers are non-null on successful bootstrap.

**Evidence target**:
- `artifacts/p8/evidence/P8C-003.json`
- function table non-null test
- required function list captured in evidence

**Allowed**:
- Implement stubbed or minimal deterministic functions only when their behavior is separately gated.
- Return `NotReady`, `UnsupportedOperation`, or `ContractGap` only where the design permits and evidence records it.

**Forbidden**:
- NULL required V1 function pointer.
- Hidden optional V1 functions.
- Marking unimplemented behavior as PASS.

**Design reference**: section 26.

### P8C-004: Command Token and Lifecycle Runtime Gate

**Goal**: Implement command allocation, rejection, query, and lifecycle snapshots.

**Evidence target**:
- `artifacts/p8/evidence/P8C-004.json`
- command token tests for accepted, rejected-before-allocation, no-op, superseded, closed
- query-by-`token.command_id` tests
- lifecycle transition evidence

**Allowed**:
- Add command lifecycle internals under `command/`.
- Use deterministic fake clocks/ticks in tests.

**Forbidden**:
- Using correlation ID as command ID.
- Allocating non-zero command IDs for pre-allocation rejection.
- Losing accepted snapshot revision.

**Design reference**: sections 6.3, 6.4.

### P8C-005: Plane State, Timeline Availability, and Revision Equality Gate

**Goal**: Implement core state and revision comparison rules.

**Evidence target**:
- `artifacts/p8/evidence/P8C-005.json`
- tests for request revision equal, older, and newer than latest state
- evidence for `TimelineInconsistent` on stale request
- proof P8 does not mutate state from `build_frame` solely due to newer request revision

**Allowed**:
- Add state/timeline internals under `core/`.
- Mirror P7/P4 revision values through P8 wrapper types.

**Forbidden**:
- Direct public dependency on P7 C++ types.
- Treating equal revision as conflict.
- Mutating global state from frame build on newer request revision.

**Design reference**: sections 12.1, 12.2, 12.3.

### P8C-006: Runtime Caps and UINT64_MAX Rejection Gate

**Goal**: Enforce runtime caps and reject unlimited cap sentinels.

**Evidence target**:
- `artifacts/p8/evidence/P8C-006.json`
- tests for `cap == UINT64_MAX` returning `MalformedInput` or `InvalidCapValue`
- runtime caps default evidence including close timeouts and hazard retry default

**Allowed**:
- Add cap validation under `core/`.
- Document defaults in evidence.

**Forbidden**:
- Treating `UINT64_MAX` as unlimited.
- Silent wraparound or allocation attempt for invalid caps.

**Design reference**: sections 22.1, 22.3, 22.4, 25.

### P8C-007: Cancellation Callback Gate

**Goal**: Replace any volatile-pointer cancellation model with callback-based polling.

**Evidence target**:
- `artifacts/p8/evidence/P8C-007.json`
- tests for null `out_cancelled`, continue, cancel, worker-thread-safe polling, and forbidden mutation callback behavior
- scan proving no `volatile uint32_t*` cancellation contract in P8 ABI

**Allowed**:
- Implement cancellation view helpers under `threading/`.
- Use deterministic test callbacks.

**Forbidden**:
- Cross-thread `volatile` synchronization contract.
- Calling mutating P8 APIs from cancellation callbacks.
- Deadlocking when callback returns cancel.

**Design reference**: section 18.

## 10. P8D - Source Reader / Discovery / Embedded Stream

### P8D-001: External Source VTable and Descriptor Runtime Gate

**Goal**: Implement source descriptor validation and external reader lifecycle.

**Evidence target**:
- `artifacts/p8/evidence/P8D-001.json`
- tests for FilePathUtf8, MemoryBytes, Reader, ProviderByteStream, EmbeddedTrack descriptor validation
- tests for open/read/close exactly once, non-seekable seek/tell handling, cancel/read race tolerance
- proof `KivoSubtitleExternalSourceVTable` begins with `KivoP8AbiHeader`

**Allowed**:
- Add source adapter internals under `source/`.
- Copy memory bytes before async parse.
- Use fake source readers in tests.

**Forbidden**:
- Raw file paths in evidence.
- Retaining caller memory without copy.
- Leaking a source handle after successful open.

**Design reference**: section 10.

### P8D-002: ResourceAccessToken Irrelevant-Field Rule Gate

**Goal**: Enforce token kind-specific fields exactly.

**Evidence target**:
- `artifacts/p8/evidence/P8D-002.json`
- tests for valid FrameLease token, valid SnapshotLease token, non-zero irrelevant fields, missing snapshot revision, malformed token

**Allowed**:
- Add token validation helpers under `frame/` or `snapshot/` only if ownership is explicit.

**Forbidden**:
- Accepting non-zero irrelevant ID/generation fields.
- Omitting `snapshot_revision`.
- Sharing one catch-all token validator file with unrelated responsibilities.

**Design reference**: section 16.

### P8D-003: Privacy Hash, Key Provider, and Redacted Source Identity Gate

**Goal**: Implement deterministic privacy-safe identity and hash rules.

**Evidence target**:
- `artifacts/p8/evidence/P8D-003.json`
- tests for keyed hash provider, redacted basename hash, source identity hash, content fingerprint hash, missing key behavior
- scan proving MD5 is not used
- evidence for deterministic fake/test-only unkeyed algorithm boundaries

**Allowed**:
- Add privacy internals under `privacy/`.
- Use deterministic fake key provider in tests.
- Use unkeyed hash only in test-only or local non-exported font-dedup contexts.

**Forbidden**:
- Raw paths, raw URLs with query, provider tokens, raw subtitle text, or unredacted font names in evidence when policy forbids them.
- MD5.
- Sorting by raw source identity.

**Design reference**: sections 9.1, 9.2, 9.3, 9.4.

### P8D-004: Discovery Priority and External Preference Gate

**Goal**: Implement V1.5.5 subtitle discovery priority ordering.

**Evidence target**:
- `artifacts/p8/evidence/P8D-004.json`
- tests covering user-added first, forced/default/language priority, `user_prefers_external_over_embedded`, embedded-before-sidecar default, exact/name/language suffix, priority_score, source_order, and `source_identity_hash` tie-break

**Allowed**:
- Add discovery ranking under `discovery/`.
- Use redacted/hash tie-breaks only.

**Forbidden**:
- Raw path tie-breaks.
- Ignoring user external preference.
- Modifying P7 discovery contracts.

**Design reference**: section 17.

### P8D-005: Format, Track, and Embedded Codec Contract Gate

**Goal**: Define and validate text/bitmap/unsupported track classification.

**Evidence target**:
- `artifacts/p8/evidence/P8D-005.json`
- tests for SRT, ASS, SSA, WebVTT, PGS, VobSub, DVBSub, unsupported format
- tests for ExternalFile, ExternalMemory, EmbeddedPacket, UnsupportedBitmap track kind
- embedded codec mapping tests for Matroska/MP4/MOV/bitmap/unsupported cases

**Allowed**:
- Add format/track helpers under `format/` and `source/`.

**Forbidden**:
- Rendering bitmap subtitles in P8 P0.
- Pretending unsupported bitmap tracks are parsed text tracks.
- Adding FFmpeg type leakage to public ABI.

**Design reference**: section 11.

### P8D-006: Embedded Packet and Stream End Gate

**Goal**: Implement embedded subtitle packet generation and stream end behavior.

**Evidence target**:
- `artifacts/p8/evidence/P8D-006.json`
- tests for EOS, discontinuity, parser drain request, stale generation ignored with evidence, post-EOS packet drop for same generation, new generation acceptance

**Allowed**:
- Add embedded stream internals under `source/`.
- Wait for parser drain only on owner/packet worker path.

**Forbidden**:
- Waiting from `build_frame`.
- Mutating parser state on stale generation without evidence.
- Dropping post-discontinuity packets incorrectly.

**Design reference**: section 20.

### P8D-007: Source Error and Evidence Gate

**Goal**: Map source failures into evidence and public errors without leaking sensitive source data.

**Evidence target**:
- `artifacts/p8/evidence/P8D-007.json`
- tests for source open failure, read failure, discovery failure, unsupported encoding, security cap exceeded
- evidence redaction samples

**Allowed**:
- Add evidence reason mapping under `evidence/` with source-owned call sites.

**Forbidden**:
- Raw paths or raw subtitle text in evidence.
- Swallowing failures as empty subtitle success.

**Design reference**: sections 9, 10, 15.

### P8D-008: Source Reader Race and Close Gate

**Goal**: Prove cancel/read/close races are bounded and do not leak handles.

**Evidence target**:
- `artifacts/p8/evidence/P8D-008.json`
- race tests for cancel during read, close after cancel, duplicate close prevention, reader non-thread-safe serialization

**Allowed**:
- Use deterministic fake source readers.
- Serialize per-handle calls in P8 P0 unless source declares concurrent-safe.

**Forbidden**:
- Concurrent calls into non-thread-safe readers.
- Double close.
- Throwing across ABI callbacks.

**Design reference**: section 10.3.

## 11. P8E - Parser Semantics / Timeline / Frame Lease

### P8E-001: Plane Lifecycle and Timeline Availability Runtime Gate

**Goal**: Implement subtitle plane lifecycle and timeline availability transitions.

**Evidence target**:
- `artifacts/p8/evidence/P8E-001.json`
- tests covering created, discovering, opening, reading, parsing, indexing, ready, priming, active, paused, seeking, flushing, track switching, EOS, timeline unavailable/not started, recovering, closing, closed, failed

**Allowed**:
- Add plane lifecycle under `core/`.

**Forbidden**:
- Collapsing distinct design states into one generic state without evidence.
- Treating timeline-not-started as fatal.

**Design reference**: section 12.

### P8E-002: Parser Input, Security Caps, and Cue Index Gate

**Goal**: Parse supported text subtitle inputs within caps and build deterministic cue indexes.

**Evidence target**:
- `artifacts/p8/evidence/P8E-002.json`
- tests for malformed input, unsupported format, invalid time range, invalid language tag, invalid region, text/string/font pool caps
- deterministic cue count and skipped/repaired counts

**Allowed**:
- Add parser semantic helpers under `format/`.
- Use fake deterministic parser coverage before real decoder/renderer integration.

**Forbidden**:
- Online subtitle search.
- OCR.
- AI subtitle generation or runtime.
- Rendering responsibility.

**Design reference**: sections 6, 11, 15, 21.

### P8E-003: Generation Freshness and Cold-Start Late Packet Gate

**Goal**: Implement generation comparison and cold-start packet retention.

**Evidence target**:
- `artifacts/p8/evidence/P8E-003.json`
- tests proving delay epoch does not invalidate parsed cues
- tests proving seek epoch does not invalidate static external cue index
- tests proving embedded packets are filtered by packet generation and seek epoch
- cold-start test proving packets are not late-dropped before valid timeline state

**Allowed**:
- Add generation helpers under `generation/`.

**Forbidden**:
- Dropping embedded packets by playback time before timeline exists.
- Treating delay changes as parser invalidation.

**Design reference**: sections 8, 22.2.

### P8E-004: SSA Behavior Gate

**Goal**: Implement SSA v4.00 behavior and unsupported ASS tag evidence.

**Evidence target**:
- `artifacts/p8/evidence/P8E-004.json`
- tests for SSA dialogue/style grammar, ASS-specific tags absent in SSA, `\N` line breaks where supported, BGR color semantics where present

**Allowed**:
- Add SSA semantic normalization under `format/`.

**Forbidden**:
- Treating SSA as fully equivalent to ASS without evidence.
- Silently accepting executable/script semantics.

**Design reference**: section 21.1.

### P8E-005: ASS Color, Alpha, and PlayRes Missing Gate

**Goal**: Implement ASS color/alpha conversion and missing PlayRes fallback.

**Evidence target**:
- `artifacts/p8/evidence/P8E-005.json`
- tests for BGR color channels, inverse alpha, channel-specific alpha override, malformed color/alpha evidence, PlayRes missing `NormalizedFallback`

**Allowed**:
- Store layout metadata for later P9 renderer decisions.

**Forbidden**:
- Inventing video pixel size when PlayRes is missing.
- Treating malformed color as silent success.

**Design reference**: sections 21.2, 21.3.

### P8E-006: WebVTT Cue Settings Gate

**Goal**: Map WebVTT cue settings into metadata or unsupported evidence.

**Evidence target**:
- `artifacts/p8/evidence/P8E-006.json`
- tests for line, position, size, align, vertical, region, snap-to-lines

**Allowed**:
- Store metadata for P9 renderer/layout.
- Emit unsupported evidence for unsupported P8 P0 semantics.

**Forbidden**:
- Implementing renderer layout in P8.
- Dropping cue settings without metadata or evidence.

**Design reference**: section 21.4.

### P8E-007: Frame Lease Acquire/Release Gate

**Goal**: Implement frame lease lifecycle and pool exhaustion behavior.

**Evidence target**:
- `artifacts/p8/evidence/P8E-007.json`
- tests for Ok, Empty, Error, Closed, PoolExhausted, Tombstoned
- tests for lease_id zero no-release, duplicate release, invalid lease, release never dereferences frame pointer

**Allowed**:
- Add frame model and lease pool under `frame/`.

**Forbidden**:
- Dereferencing opaque frame pointer in release.
- Blocking indefinitely on frame lease acquisition.
- Freeing visible memory behind active hazard pointer.

**Design reference**: section 13.

### P8E-008: Frame Hot Path Non-Blocking Gate

**Goal**: Prove frame build/acquire path has no mutex/shared_mutex/blocking wait.

**Evidence target**:
- `artifacts/p8/evidence/P8E-008.json`
- architecture scan for frame hot path blocking primitives
- tests for temporarily unavailable policy under contention

**Allowed**:
- Use bounded atomics, RCU/hazard retry, and deterministic fake contention tests.

**Forbidden**:
- `std::mutex`, `std::shared_mutex`, condition-variable wait, blocking I/O, or parser drain waits on frame hot path.
- Hiding blocking behavior in helper files.

**Design reference**: sections 22.4, 25, 31.

### P8E-009: EOS Epsilon and Hazard Retry Bound Gate

**Goal**: Implement EOS hold epsilon and hazard retry exhaustion behavior.

**Evidence target**:
- `artifacts/p8/evidence/P8E-009.json`
- tests proving EOS epsilon uses `subtitle_time_compare_epsilon_us` from runtime caps
- tests proving frame duration hint clamp policy where present
- tests proving default `rcu_hazard_acquire_max_retries == 3`
- tests for frame build and snapshot acquire retry exhaustion

**Allowed**:
- Add hazard retry policy under `threading/`.

**Forbidden**:
- Hardcoding unrelated epsilon.
- Infinite retry loops.
- Blocking waits on retry exhaustion.

**Design reference**: sections 22.3, 22.4.

## 12. P8F - Snapshot / Evidence / Metrics / Observer / Close

### P8F-001: Snapshot Lease Lifecycle Gate

**Goal**: Implement snapshot lease acquire/release and statuses.

**Evidence target**:
- `artifacts/p8/evidence/P8F-001.json`
- tests for Ok, Empty, Error, Closed, Tombstoned, invalid lease, late release, duplicate release

**Allowed**:
- Add snapshot lease internals under `snapshot/`.

**Forbidden**:
- Treating snapshot lease as direct text/cue buffer access.
- Deadlocking on leaked snapshot lease.

**Design reference**: sections 14.1, 14.2, 25.

### P8F-002: Snapshot Blob Binary Layout Gate

**Goal**: Implement self-contained snapshot blob header and section table.

**Evidence target**:
- `artifacts/p8/evidence/P8F-002.json`
- tests for magic `P8SB`, revision, alignment, section table offsets, total bytes, TrackArray, EvidenceSummaryArray, FontAttachmentArray, CommandSummaryArray
- tests proving metrics are inline, selected track inline, string table not embedded, text buffers not embedded, cue text only accessible from FrameLease

**Allowed**:
- Add serializer under `snapshot/`.
- Copy snapshot into `KivoMutableByteSpan`.

**Forbidden**:
- Embedding string table or text buffers in P8 P0 snapshot blob.
- Leaving referenced snapshot types undefined.
- Writing host-endian ambiguous blobs.

**Design reference**: sections 14.3, 14.4, 14.5, 14.6.

### P8F-003: Snapshot Endian Responsibility Gate

**Goal**: Enforce little-endian P8 P0 snapshot responsibility.

**Evidence target**:
- `artifacts/p8/evidence/P8F-003.json`
- tests or compile assertions proving Windows x64 little-endian baseline
- get_api unsupported endian behavior where testable
- snapshot copy emits little-endian blob

**Allowed**:
- Use compile-time endian checks.
- Mark big-endian runtime as unsupported in P8 P0.

**Forbidden**:
- Claiming byte-swap support in P8 P0.
- Emitting undocumented host-endian blobs.

**Design reference**: sections 14.6, 24.

### P8F-004: Evidence Summary and Metrics Gate

**Goal**: Implement evidence severity/reason summaries and metrics snapshot.

**Evidence target**:
- `artifacts/p8/evidence/P8F-004.json`
- tests for severity aggregation, primary evidence, counts, overflow counters, packet queue metrics, lease counts, text/font/string pool bytes, parse time metrics

**Allowed**:
- Add evidence and metrics internals under `evidence/`.

**Forbidden**:
- Raw private data in evidence.
- Reporting metrics not backed by state.
- Collapsing all reasons into `ContractGap`.

**Design reference**: section 15.

### P8F-005: Observer Callback and Subscription Gate

**Goal**: Implement observer callbacks safely and versioned.

**Evidence target**:
- `artifacts/p8/evidence/P8F-005.json`
- tests for null callbacks invalid token, on_event delivery, mutating API rejection from observer thread, unsubscribe inside callback effective after return, non-blocking delivery policy, queue overflow evidence

**Allowed**:
- Add observer registry under `observer/`.
- Use `KivoSubtitleSubscriptionToken`.

**Forbidden**:
- Observer callback calling mutating APIs without rejection.
- Deadlock on unsubscribe from callback.
- Blocking observer delivery.

**Design reference**: section 19.

### P8F-006: Close Tombstone and Leak-Safe Gate

**Goal**: Implement bounded close behavior for frame and snapshot leases.

**Evidence target**:
- `artifacts/p8/evidence/P8F-006.json`
- tests for frame lease close timeout, snapshot lease close timeout, tombstone after timeout, late release safe/no-op, leaked lease no deadlock, minimal tombstone metadata retained

**Allowed**:
- Add close policy under `threading/` and lease-owned tombstone records under `frame/` and `snapshot/`.

**Forbidden**:
- Indefinite close wait.
- Freeing memory still visible through active hazard pointer.
- Keeping sensitive data in tombstone records.

**Design reference**: sections 14.6, 25.

### P8F-007: Text Buffer and Font Blob Access Gate

**Goal**: Implement text/font blob identifiers and access rules without snapshot blob embedding.

**Evidence target**:
- `artifacts/p8/evidence/P8F-007.json`
- tests for `KivoSubtitleTextBufferId`, `KivoSubtitleFontBlobId`, buffer-too-small, not-found, pool/store exceeded, SnapshotLease font copy, FrameLease cue text access

**Allowed**:
- Add text/font stores under `text/`.

**Forbidden**:
- Embedding text buffers in snapshot blob.
- Exposing raw font names in evidence when redaction policy forbids it.
- Letting SnapshotLease expose cue text.

**Design reference**: sections 5, 14.4, 15.

### P8F-008: Privacy Evidence Redline Gate

**Goal**: Prove evidence and diagnostics honor redaction policy.

**Evidence target**:
- `artifacts/p8/evidence/P8F-008.json`
- redline scan for raw paths, subtitle text, font names, URL query, provider tokens, MD5
- privacy mode tests for Strict, DiagnosticRedacted, DiagnosticVerboseLocalOnly

**Allowed**:
- Add privacy-specific evidence tests.

**Forbidden**:
- Raw secrets or private media paths in evidence artifacts.
- Treating local verbose diagnostics as export-safe.

**Design reference**: sections 9.3, 15, 31.

## 13. P8G - Tooling / Guards / Required Tests

### P8G-001: Contract Manifest and No-Omission Tool Gate

**Goal**: Generate machine-checkable coverage for design IDs, tests, and exit criteria.

**Evidence target**:
- `artifacts/p8/evidence/P8G-001.json`
- `docs/design/p8/KIVO-VIDEO-P8-SUBTITLE-PLANE-DESIGN-LOCK-001-V1.5.5.rule_manifest.yaml`
- `docs/roadmap/p8_exit_criteria_s1_s30_matrix.md`
- report proving C/G/A/I items and S1-S30 map to gates and tests

**Allowed**:
- Add matrix tooling under `backend/tools/video/subtitle_plane/matrix_codegen/`.

**Forbidden**:
- Manual-only closure with no scanable coverage artifact.
- Dropping earlier carryover tests.

**Design reference**: sections 27, 28, 30.

### P8G-002: Public C Header Purist Tool Gate

**Goal**: Automate all public C header forbidden-token checks.

**Evidence target**:
- `artifacts/p8/evidence/P8G-002.json`
- tool report for no bare `noexcept`, no `std::`, no C++ ABI tokens, no Qt/FFmpeg/Win32 renderer tokens, no `KivoInputBytesView`, no volatile cancellation pointer

**Allowed**:
- Add scanner under `backend/tools/video/subtitle_plane/c_header_purist/`.

**Forbidden**:
- Whitelisting production violations without user-approved design exception.
- Reporting PASS with unsupported scanner schema.

**Design reference**: sections 2, 7, 18, 27.

### P8G-003: ABI Layout Guard Tool Gate

**Goal**: Automate public struct size/offset/alignment/static assert coverage.

**Evidence target**:
- `artifacts/p8/evidence/P8G-003.json`
- generated sizeof/offsetof report
- static asserts for all public structs
- MSVC and clang-cl comparison when available
- documented small-wrapper exceptions

**Allowed**:
- Add guard under `backend/tools/video/subtitle_plane/abi_layout_guard/`.
- Add compile tests under `backend/tests/video/subtitle_plane/abi/`.

**Forbidden**:
- Omitting any public struct.
- Compiler-specific packing.
- Treating one compiler as cross-compiler proof without noting boundaries.

**Design reference**: section 23.

### P8G-004: Bootstrap and Function Table Required Tests Gate

**Goal**: Close required bootstrap/function table tests from V1.5.5.

**Evidence target**:
- `artifacts/p8/evidence/P8G-004.json`
- tests:
  - `c_header_compiles_as_c`
  - `bootstrap_returns_status_not_bool`
  - `api_required_prefix_size_constant`
  - `get_api_v1_prefix_too_small`
  - `get_api_v1_null_out_api`
  - `get_api_v1_unsupported_revision`
  - `function_table_all_v1_functions_non_null`

**Allowed**:
- Add focused ABI tests only.

**Forbidden**:
- Replacing CTest with direct exe only.
- Counting compile-only scan as runtime behavior.

**Design reference**: section 27.

### P8G-005: Deterministic Fake Runtime Harness Gate

**Goal**: Provide deterministic tests for source, parser, timeline, frame, snapshot, observer, and privacy behavior.

**Evidence target**:
- `artifacts/p8/evidence/P8G-005.json`
- fake source reader, fake key provider, fake clock, fake timeline, fake packet source, fake observer support list
- tests that run without real media, renderer, Qt, FFmpeg, OCR, online search, or AI runtime

**Allowed**:
- Add test support under `backend/tests/video/subtitle_plane/test_support/`.

**Forbidden**:
- Requiring hardware/media/network for contract tests.
- Importing production-only dependencies into deterministic fake tests.

**Design reference**: sections 10, 15, 18, 19, 27.

### P8G-006: Race, Property, and Malformed Input Test Gate

**Goal**: Stress the contracts most likely to fail after initial implementation.

**Evidence target**:
- `artifacts/p8/evidence/P8G-006.json`
- source cancel/read/close race tests
- snapshot buffer malformed tests
- resource token malformed tests
- hazard retry exhaustion tests
- observer callback mutation tests
- cap boundary tests

**Allowed**:
- Add bounded deterministic stress/property tests.

**Forbidden**:
- Unbounded flaky sleeps.
- Infinite race loops.
- Marking nondeterministic failures as PASS.

**Design reference**: sections 10, 14, 16, 18, 19, 22, 25.

### P8G-007: Architecture Guard Gate

**Goal**: Prove P8 stays inside its family and does not modify frozen contracts or pull forbidden dependencies into public ABI.

**Evidence target**:
- `artifacts/p8/evidence/P8G-007.json`
- architecture guard report for P8 paths, public/private dependency boundaries, forbidden root files, dumping-ground names, P4/P5/P6/P7 frozen public contract diff

**Allowed**:
- Add guard under `backend/tools/video/subtitle_plane/architecture_guard/`.

**Forbidden**:
- P8 code in root-level files.
- Public ABI dependency on private runtime.
- Frozen P4/P5/P6/P7 public contract edits without explicit user approval.

**Design reference**: sections 0, 28, 31.

### P8G-008: Earlier Version Required Test Carryover Gate

**Goal**: Preserve all verified V1.5.4/V1.5.3/V1.5.2 required tests.

**Evidence target**:
- `artifacts/p8/evidence/P8G-008.json`
- carryover tests list from P8A-003
- pass/fail/not-verified status per carryover test
- conflict notes where V1.5.5 supersedes earlier wording

**Allowed**:
- Implement carried tests if verified.
- Mark missing source as `BLOCKED_ENV` only when P8A already recorded missing baseline evidence and user-approved replacement.

**Forbidden**:
- Dropping earlier required tests silently.
- Treating unverified memory as current design truth.

**Design reference**: section 27.

## 14. P8H - Final Validation / Closure / Delivery Readiness

### P8H-001: Accumulated Build and CTest Gate

**Goal**: Validate P8 after all implementation and tooling gates.

**Evidence target**:
- `artifacts/p8/evidence/P8H-001.json`
- configure/build command output
- `ctest --preset vs-debug --output-on-failure --timeout 60` or approved preset equivalent
- Total/Passed/Failed test count
- list of P8 test executables and direct exe mapping only if direct runs are used as supplemental evidence

**Allowed**:
- Run full accumulated validation.
- Classify environment blockers honestly.

**Forbidden**:
- Direct exe as replacement for CTest without evidence.
- Reporting PASS with failed or skipped required tests.
- Hiding failed non-P8 tests if they are caused by P8 changes.

**Design reference**: sections 27, 28, 29.

### P8H-002: Redline and Forbidden Modification Gate

**Goal**: Prove no privacy leak, forbidden dependency leak, or frozen contract modification remains.

**Evidence target**:
- `artifacts/p8/evidence/P8H-002.json`
- full redline scan report
- forbidden token report
- frozen public contract diff report for P4/P5/P6/P7
- P8 public ABI dependency report

**Allowed**:
- Run scans and generate reports.
- Fix production pollution if discovered.

**Forbidden**:
- Ignoring any production redline hit.
- Claiming no frozen modification without diff evidence.
- Treating comments/sentinels as production-safe without classification.

**Design reference**: sections 0, 2, 9, 28, 31.

### P8H-003: Evidence Consistency and S1-S30 Closure Gate

**Goal**: Ensure evidence, card, reports, and implementation agree.

**Evidence target**:
- `artifacts/p8/evidence/P8H-003.json`
- S1-S30 checklist with status and evidence file per item
- C/G/A/I delta checklist with evidence file per item
- no-omission matrix
- evidence/card consistency scan

**Allowed**:
- Generate final matrices.
- Correct evidence metadata mismatches.

**Forbidden**:
- Claiming closure with unmapped criteria.
- Changing evidence status language outside the approved vocabulary.
- Marking `SCHEMA_DEGRADED` as PASS.

**Design reference**: sections 28, 29, 30.

### P8H-003R: Source Design Line-by-Line Reconciliation Gate

**Goal**: Re-read the original V1.5.5 DOCX after implementation and prove every design sentence/numbered rule is represented by implementation, test, evidence, or an explicit non-goal/blocker.

**Evidence target**:
- `artifacts/p8/evidence/P8H-003R.json`
- regenerated DOCX extraction with paragraph count and SHA-256
- paragraph/rule-to-artifact reconciliation table
- all sections 0..31 checked, including C-001..C-005, G-001..G-015, A-001..A-005, I-001..I-003, required tests, exit criteria, final report requirements, closure definition, and final delivery statement

**Allowed**:
- Re-read the original DOCX from `C:\Users\huyasi\Downloads\Kivo Video P8 Subtitle Plane Design V1.5.5.docx`.
- Generate reconciliation artifacts under `artifacts/p8/evidence/` and `docs/roadmap/`.
- Fix any discovered omissions before final closure.

**Forbidden**:
- Submitting or pushing before this reconciliation is complete.
- Treating the execution card alone as proof of design coverage.
- Collapsing unimplemented design text into a generic PASS.
- Ignoring a mismatch because tests happen to pass.

**Design reference**: sections 0..31.

### P8H-004: Final Report Gate

**Goal**: Produce the P8 final report only after validation is complete.

**Evidence target**:
- `artifacts/p8/evidence/P8H-004.json`
- `docs/roadmap/p8_final_closure_report.md`
- report includes branch, baseline commit, final head, remote head, changed files, public C header list, C++ wrapper header list if any, private runtime files, private test support files, C header compile result, no-bare-noexcept scan, no-std scan, forbidden token scan, bootstrap symbol result, required prefix test, function table non-null test, all public type closure evidence, source vtable evidence, observer callback evidence, snapshot blob section evidence, privacy hash evidence, cancellation callback evidence, ABI layout evidence, close tombstone evidence, ASS/SSA/WebVTT semantic evidence, contract gap report, S1-S30 checklist, and readiness statement

**Allowed**:
- Write final closure report after P8H-001..003 pass or are honestly blocked.

**Forbidden**:
- Writing "P8 complete" before required gates pass.
- Omitting contract gaps.
- Claiming runtime proof from static scans.

**Design reference**: section 29.

### P8H-005: Delivery and Commit Readiness Gate

**Goal**: Confirm P8 can be delivered or identify exact blockers.

**Evidence target**:
- `artifacts/p8/evidence/P8H-005.json`
- final git status
- final changed file list grouped by natural family
- ready statement:
  - `P8_COMPLETE_READY_FOR_P9` only if all required gates pass and no blockers remain, or
  - `P8_BLOCKED_WITH_EVIDENCE` with exact blockers

**Allowed**:
- Prepare staged changes or commit only if the user explicitly requests it.
- State readiness for P9 only when evidence supports it.

**Forbidden**:
- Premature handoff.
- Starting P9.
- Hiding dirty unrelated files.

**Design reference**: sections 30, 31.

## 15. Global Allowed File Families by Stage

| Stage | Allowed write families |
|---|---|
| P8A | `docs/roadmap/p8*.md`, `artifacts/p8/evidence/P8A-*.json`, optional `docs/design/p8/*.md|*.yaml` only after source extraction gate |
| P8B | `include/kivo/video/subtitle_plane/kivo_subtitle_plane.h`, `backend/tests/video/subtitle_plane/abi/**`, `backend/tools/video/subtitle_plane/c_header_purist/**`, `backend/tools/video/subtitle_plane/abi_layout_guard/**`, `artifacts/p8/evidence/P8B-*.json` |
| P8C | `backend/src/video/subtitle_plane/abi|command|core|threading/**`, matching private headers/tests, CMake entries strictly needed for P8, `artifacts/p8/evidence/P8C-*.json` |
| P8D | `backend/src/video/subtitle_plane/source|privacy|discovery|format|evidence/**`, matching private headers/tests, `artifacts/p8/evidence/P8D-*.json` |
| P8E | `backend/src/video/subtitle_plane/core|format|generation|frame|threading/**`, matching private headers/tests, `artifacts/p8/evidence/P8E-*.json` |
| P8F | `backend/src/video/subtitle_plane/snapshot|evidence|metrics|observer|privacy|text|threading/**`, matching private headers/tests, `artifacts/p8/evidence/P8F-*.json` |
| P8G | `backend/tools/video/subtitle_plane/**`, `backend/tests/video/subtitle_plane/**`, `docs/design/p8/**`, `docs/roadmap/p8_exit_criteria_s1_s30_matrix.md`, `artifacts/p8/evidence/P8G-*.json` |
| P8H | `docs/roadmap/p8_final_closure_report.md`, final matrix/report updates, `artifacts/p8/evidence/P8H-*.json` |

Any file outside these families requires a gate-specific written reason in evidence before editing.

## 16. Global Forbidden Areas

Forbidden unless a later user-approved gate explicitly overrides:

- P4/P5/P6/P7 frozen public contract rewrites.
- UI, Qt adapter, renderer, Direct3D/Win32 renderer, OCR, online subtitle search, AI subtitle runtime.
- Public C ABI exposure of C++ STL, Qt, FFmpeg, Win32 renderer, Direct3D, DXGI, COM smart pointers, exceptions, bare `noexcept`, `bool` ABI returns where status is required, `size_t` ABI byte counts where `uint64_t` is required.
- Root-level scratch files and generated reports.
- New `common`, `misc`, `utils`, `helpers`, `shared` dumping-ground families.
- Raw path, raw subtitle text, raw URL query, provider token, or unredacted font name leakage in exportable evidence.
- Direct test execution used as a replacement for CTest in final closure.

## 17. Stop Conditions

Stop the current stage immediately if any of the following happens:

- A gate fails.
- A required source baseline is missing and no user-approved replacement decision exists.
- A public C ABI referenced type remains undefined.
- C header does not compile as C.
- Public C ABI forbidden token scan hits production code.
- A P4/P5/P6/P7 frozen public contract change appears without explicit approval.
- Any implementation file violates single responsibility or lands outside the approved family tree.
- Evidence JSON and execution card disagree on task ID, gate ID, status, allowed files, or covered exit criteria.
- Privacy redline finds production leakage of raw path, subtitle text, URL query, provider token, or unredacted font name.
- Test tooling is unavailable and the gate attempts to report PASS instead of `BLOCKED_ENV` or `SCHEMA_DEGRADED`.

## 18. Professional Start Checklist

Before starting P8A:

1. Confirm this execution card is the only file created by the current planning task.
2. Confirm `C:\kivo video` is the active working directory.
3. Confirm the source DOCX hash matches `9AC07D575468533312CB403CED5CD368372F2DC248CC5232F3B492B8F72B4197`.
4. Confirm dirty state is recorded and no user/prior-agent changes are reverted.
5. Start with `P8A-001`; do not skip to P8B.
6. Do not claim P8 implementation completion until P8H closes with evidence.
