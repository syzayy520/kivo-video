# P2 Source Runtime Final Closure 001 Execution Card

Updated: 2026-06-27

Canonical task id: `KIVO-VIDEO-P2-SOURCE-RUNTIME-FINAL-CLOSURE-001`.
Short task id: `P2-SOURCE-RUNTIME-FINAL-CLOSURE-001`.
Design freeze version: `V7-ULTIMATE-LOCAL-SAFE-ISOLATED-WORKTREE`.
Final closure evidence target: `artifacts/p2/evidence/P2-SOURCE-RUNTIME-FINAL-CLOSURE-001.json`.
Final closure card target generated by the executor: `docs/execution_cards/P2_SOURCE_RUNTIME_FINAL_CLOSURE_001.md`.
This roadmap handoff card: `docs/roadmap/p2_source_runtime_final_closure_001_execution_card.md`.
Design baseline source: `KIVO-VIDEO-P2-SOURCE-RUNTIME-FINAL-CLOSURE-001-V7-ULTIMATE-LOCAL-SAFE-ISOLATED-WORKTREE`.

This card converts the V7 frozen closure task into a directly executable assistant task. The DOCX at `C:\Users\Administrator\Downloads\KIVO-VIDEO-P2-SOURCE-RUNTIME-FINAL-CLOSURE-001-V7-ULTIMATE-LOCAL-SAFE-ISOLATED-WORKTREE.docx` is the design authority for this task.

## Critical Clarification

This roadmap card is not the final closure execution card. It is the instruction card that tells the next assistant how to perform the closure.

The assistant executing this task must generate, inside the isolated closure worktree after real verification:

- `artifacts/p2/evidence/P2-SOURCE-RUNTIME-FINAL-CLOSURE-001.json`
- `docs/execution_cards/P2_SOURCE_RUNTIME_FINAL_CLOSURE_001.md`

Do not pre-fill those final artifacts with fake PASS values. They must be generated only after the actual gates pass.

## Position

This is the only remaining P2 Source Runtime final closure task. It is:

- final closure.
- baseline lock.
- verification-only unless a true P2 source runtime bug blocks closure.
- evidence freeze.
- isolated-worktree-first.
- local-safe.
- staged approval.
- repair-allowed-if-correct.
- timeout-protected.
- no new provider.
- no runtime feature expansion.
- no redesign.
- no FFmpeg, UI, render, audio, cache, prefetch, benchmark, or P3 work.

This task completes only if it can honestly say:

- `P2 closed.`
- `P2 source runtime baseline locked.`
- `Ready for P3 planning / P3 implementation.`

## Execution Order Dependency

Execute only after these tasks are complete or their exact equivalent evidence exists:

1. `SRC-CORE-SKELETON-001`
2. `LOCAL-FILE-SOURCE-RUNTIME-001`
3. `REMOTE-RANGE-RUNTIME-HTTP-WEBDAV-001`

The closure baseline assumes:

- local_file baseline commit `c7f82900b0124ca91277fad97782999428a7a6bd`.
- HTTP/WebDAV repair baseline commit `c90cfdc938cd21e6ecf6d62d1b088d0d0fe3b9a1`.
- `c90cfdc` commit message exactly `fix(http-webdav): repair remote range strict gates`.
- remote evidence remains `p2-evidence-v15`, `V1.5-FINAL-R1`, fixture level `level_1_deterministic_transport_fixture`, `loopback_socket_runtime: not_verified`, `production_transport_complete: not_claimed`, total tests 86 passed 86 failed 0.

STOP if these assumptions cannot be proven.

## Local Project Protection Is First

Priority order:

`local project safety > P2 closure > commit > push > GitHub repository`

Original project path:

`C:\kivo video`

The original worktree is read-only for this closure task. It must not be modified, used to generate closure artifacts, committed, pushed, reset, cleaned, restored, or deleted.

Original worktree allowed operations:

- read status.
- read HEAD.
- read branch.
- read log.
- read remotes.
- read worktree list.

Original worktree forbidden operations:

- `git reset --hard`
- `git clean -fd`
- `git clean -fdx`
- `git checkout -f`
- `git switch -f`
- `git restore .`
- `git restore --source`
- `git rm`
- `git merge`
- `git rebase`
- `git cherry-pick`
- `git push --force`
- `git push --mirror`
- deleting `C:\kivo video`
- deleting `backend/**`, `artifacts/**`, `docs/**`, or `.git/**`

Exception:

- `git reset -q -- <path>` is allowed only to undo `git add -N` intent-to-add state. It must never discard file content or roll back source changes.

## Default Mode

Use an isolated git worktree by default.

Recommended worktree path from original project parent:

`..\kivo-video-p2-source-runtime-final-closure-001-worktree`

Closure branch:

`kivo-video-p2-source-runtime-final-closure-001`

Worktree creation must start from:

`c90cfdc938cd21e6ecf6d62d1b088d0d0fe3b9a1`

Before creating worktree:

- closure worktree path must not already exist.
- local closure branch must not already exist.
- remote closure branch must not already exist.
- start commit must exist.
- original worktree state is recorded but does not need to be clean.

If worktree path already exists:

- STOP.
- do not delete it.
- do not overwrite it.
- do not use `Remove-Item`.
- do not use `rm -rf`.
- ask the user to choose a new path or inspect the existing worktree.

If worktree creation fails:

- STOP.
- do not switch or modify the original worktree.
- do not continue in-place.
- report failure reason and wait for user approval for any fallback.

## In-Place Fallback Gate

In-place fallback is forbidden unless every condition is true:

- the user explicitly asks to execute in the current directory.
- current branch is `kivo-video-p2-remote-range-runtime-http-webdav-001`.
- current HEAD is `c90cfdc938cd21e6ecf6d62d1b088d0d0fe3b9a1`.
- `git status --short --untracked-files=all` is empty.
- same-named closure branch does not exist locally or remotely.
- user explicitly accepts that current worktree will create branch, generate files, commit, and push.

If current worktree is not clean:

- STOP.
- do not reset.
- do not clean.
- do not restore.
- do not delete.
- recommend isolated worktree.

## Final Closure Scope

This task only does:

- closure.
- verification.
- evidence freeze.
- final execution card generation.
- final closure evidence generation.
- minimal correct P2 source runtime bug fix if required.
- closure commit.
- closure branch push.

This task does not split into:

- P2 provider resolver.
- P2 Emby foundation.
- P2 Alist foundation.
- P2 cloud drive foundation.
- P2 more HTTP polish.
- P2 more WebDAV polish.
- P2 cache.
- P2 FFmpeg probe.
- P2 DirectPlay decision.
- P2 DirectStream decision.

All of those are P3 or later.

## FC-001 V7 Rule Gate

Evidence target: final report records that V7 replaces all previous versions and that its key changes are enforced:

- isolated worktree first.
- original worktree protected even if dirty.
- in-place only by explicit user approval.
- true P2 source runtime bug fixes allowed but no feature expansion.
- HTTP/WebDAV/local_file true bug repair is not misclassified as polish.
- placeholder gate does not false-positive WebDAV XML or Markdown footnotes.
- direct exe gate has timeout, working directory, and logs.
- evidence hashes are compared before/after.
- untracked files are reviewed with reversible `git add -N`.
- commit count supports model A/B/C.
- forbidden scan hits are manually classified.
- CTest count targets matrix coverage, with 86 as current baseline target.
- final report records environment, time, version, commit model, timeout, hash, and scope.

## FC-002 Read-Only Original Worktree Gate

Evidence target:

- original root path.
- original branch.
- original HEAD.
- original status with untracked files.
- last 5 commits.
- `git worktree list`.

Original worktree dirty status is not an automatic fail. Dirty original worktree means the executor must use isolated worktree and must not clean/reset/restore it.

## FC-003 PowerShell Robust Helper Gate

Evidence target: executor uses robust command wrappers for expected failing probes.

Required helper concepts:

- `Invoke-Probe` captures native output and exit code without throwing on expected failure.
- `Invoke-Required` fails immediately on required command failure.
- `Get-Sha256` hashes required evidence paths and fails if file is missing.
- `Invoke-GitRemoteProbe` disables interactive prompts and uses low-speed HTTP safety options.

All native command exit codes must be explicitly checked. Do not rely only on log text.

## FC-004 Phase A Read-Only Probe Gate

Allowed in Phase A:

- `git status`
- `git branch`
- `git rev-parse`
- `git log`
- `git cat-file`
- `git show`
- `git merge-base`
- `git rev-list`
- `git ls-remote`
- `git symbolic-ref`
- `git worktree list`
- version commands for git/cmake/ctest/python

Forbidden in Phase A:

- `git checkout -b`, `git switch`, `git add`, `git commit`, `git push`
- `git reset`, `git clean`, `git restore`, `git rm`
- `git merge`, `git rebase`, `git cherry-pick`, `git tag`
- `cmake --build`
- `Remove-Item`, `rm -rf`, `New-Item`, `Set-Content`, `Out-File`

## FC-005 Environment Record Gate

Evidence target:

- ExecutionStartTime.
- PowerShellVersion.
- GitVersion.
- CMakeVersion.
- CTestVersion.
- PythonVersion.

The final report must also record ExecutionEndTime.

## FC-006 Start Commit Gate

Verify:

- `c7f82900b0124ca91277fad97782999428a7a6bd` exists.
- `c90cfdc938cd21e6ecf6d62d1b088d0d0fe3b9a1` exists.
- `c90cfdc` commit message is exactly `fix(http-webdav): repair remote range strict gates`.
- `c90cfdc` has a parent and is not orphan.

STOP if any proof is missing.

## FC-007 Remote Repair Branch Probe Gate

Read-only remote probe:

- `origin/kivo-video-p2-remote-range-runtime-http-webdav-001` must point to `c90cfdc938cd21e6ecf6d62d1b088d0d0fe3b9a1`.

If remote probe fails:

- STOP.
- do not continue with guessed branch state.

## FC-008 Closure Branch Non-Existence Gate

Verify:

- local branch `kivo-video-p2-source-runtime-final-closure-001` does not exist.
- remote branch `kivo-video-p2-source-runtime-final-closure-001` does not exist.

If either exists:

- STOP.
- do not overwrite.
- do not force push.

## FC-009 Default Base Branch Gate

Resolve `BASE_REMOTE` in this order:

1. `KIVO_BASE_REMOTE` environment variable if set.
2. `origin/HEAD`.
3. fallback candidates: `origin/main`, `origin/master`, `origin/develop`, `origin/trunk`.

Requirements:

- `BASE_REMOTE` must exist.
- final report records `BASE_REMOTE` and derived `BASE_LOCAL`.

## FC-010 Lineage Gate

Verify:

- `c7f829` is an ancestor of `c90cfdc`.
- `c90cfdc` and `BASE_REMOTE` have a merge-base.
- `c90cfdc` is not orphan.
- repository is not shallow.

If repository is shallow:

- STOP.
- do not automatically unshallow.
- tell user that continuing requires explicit approval for `git fetch --unshallow` or `git fetch --deepen=<N>`.

## FC-011 Commit Count Model Gate

Allowed models:

- Model A: no bug fix, one closure commit; `c90cfdc..HEAD` count = 1; commit contains closure evidence and closure execution card only.
- Model B: bug fix in same closure commit; count = 1; commit contains closure evidence/card plus allowed minimal P2 source runtime bug fix; execution card records `Minimal fixes: YES`.
- Model C: independent repair commit(s) then closure commit; final count = repair commit count + 1; every repair commit is minimal, P2 source runtime scoped, verified, and documented.

Model C only if:

- bug fix needs independent review.
- bug fix passed its gate.
- repair commit does not add provider.
- repair commit does not enter P3.
- repair commit does not modify unrelated files.
- repair commit does not change WebDAV not_verified claims.

Final report must record:

- `COMMIT_MODEL=A/B/C`
- `REPAIR_COMMIT_COUNT`
- `CLOSURE_DOC_COMMIT_COUNT=1`
- `TOTAL_COMMITS_FROM_C90`

## FC-012 Isolated Worktree Creation Gate

Create:

- worktree path: `kivo-video-p2-source-runtime-final-closure-001-worktree`.
- branch: `kivo-video-p2-source-runtime-final-closure-001`.
- base: `c90cfdc938cd21e6ecf6d62d1b088d0d0fe3b9a1`.

After creation:

- current directory is closure worktree.
- branch is closure branch.
- HEAD equals `c90cfdc938cd21e6ecf6d62d1b088d0d0fe3b9a1`.
- `git status --short --untracked-files=all` is clean.

## FC-013 Historical Diff Scope Gate

Compare `c7f829...HEAD`.

Allowed historical scope:

- `backend/include/kivo/video/source_core/**`
- `backend/src/source_core/**`
- `backend/tests/source_core/**`
- `backend/include/kivo/video/providers/local_file/**`
- `backend/src/providers/local_file/**`
- `backend/tests/providers/local_file/**`
- `backend/include/kivo/video/providers/http/**`
- `backend/src/providers/http/**`
- `backend/tests/providers/http/**`
- `backend/include/kivo/video/providers/webdav/**`
- `backend/src/providers/webdav/**`
- `backend/tests/providers/webdav/**`
- `backend/tools/gates/**`
- `backend/tools/governance/**`
- specific backend CMakeLists files for source_core/local_file/http/webdav/tests.
- `artifacts/p2/evidence/**`
- `docs/execution_cards/**`
- local/remote source runtime final reports.

Forbidden historical scope:

- providers/emby, providers/alist, providers/cloud_drive, providers/hls.
- player, decoder, render, ffmpeg.
- `ui/**`, `frontend/**`, `qml/**`.

Failure classification:

`FAIL_P2_SOURCE_RUNTIME_FINAL_CLOSURE_SCOPE`

## FC-014 Baseline Inventory Gate

local_file inventory must prove:

- local_file include/src/tests files exist.
- tokens or equivalent names prove provider/store/read result/source_changed/access_ref/DirectInput/basic_thread_safety baseline.
- equivalent renames must be documented.
- no inventory-driven feature addition.

HTTP inventory must prove:

- HTTP range open/probe/read/positioned_read/seek/close.
- 206 proof.
- 200 no-range not seekable.
- 416 and 416 */1 nonconformant semantics.
- unknown-length not seekable.
- zero read no network.
- seek cursor only.
- timeout/cancel.
- auth/header/url redaction.
- redirect redaction.
- source_changed/validator.
- DirectInput access_ref/stale.
- raw internal URL only for provider transport.
- redacted URL only for evidence/log/direct safe label.
- ProviderInternalRemoteUri move-only or equivalent secure clear.

WebDAV inventory must prove:

- WebDAV range bridge, positioned_read, seek, DirectInput, redaction, auth handling.
- The following remain not_verified:
  - WebDAV href exact-match.
  - WebDAV collection rejection.
  - WebDAV malformed XML fail-closed.
  - WebDAV large XML body cap.

Do not add WebDAV profile completeness, XML hardening, collection rejection, or related verification in this closure task.

## FC-015 Evidence Freeze Hash Gate

Before closure artifacts or bug fixes:

- hash `artifacts/p2/evidence/LOCAL-FILE-SOURCE-RUNTIME-001.json`.
- hash `artifacts/p2/evidence/REMOTE-RANGE-RUNTIME-HTTP-WEBDAV-001.json`.

After closure artifacts and any allowed bug fix:

- hash both again.
- compare before/after.

Default:

- existing local_file evidence hash must not change.
- existing remote range evidence hash must not change.

If a hash changes:

- `KIVO_EVIDENCE_REPAIR_APPROVED=true` must be set.
- before/after hash must be recorded.
- repair reason must prove it is not fabrication, repackaging, or not_verified -> verified laundering.

## FC-016 Build Freshness Gate

Use a unique build directory:

`build/backend-p2-source-runtime-final-closure-001-<timestamp>`

Rules:

- do not delete existing build directories.
- do not reuse old exe.
- do not copy results from old build directories.
- if build dir appears in git status, STOP and do not add it.

## FC-017 CTest Matrix Gate

Configure and build from `backend` into the fresh build root.

CTest must support multi-config or single-config discovery; record mode.

Current target matrix:

- local_file: 12
- HTTP range: 39
- WebDAV bridge: 15
- source_core contracts: 9
- source_core runtime: 7
- evidence validator: 1
- malicious evidence: 1
- redaction sentinel: 1
- no debug dialog: 1

Current target total: `86`.

Acceptance:

- total 86 and all pass: PASS.
- total > 86: not automatic fail, but must prove new tests are bug-fix/governance/renaming/equivalent coverage, not new P2 feature expansion.
- total < 86: STOP unless equivalent renamed/merged coverage proves full target matrix.
- failed > 0: FAIL.
- skipped > 0: explain; if it affects P2 source runtime, FAIL.
- do not remove failing tests to manufacture PASS.
- do not weaken assertions to manufacture PASS.

Final report records CTestMode, Total, Passed, Failed, Skipped, MatrixCoverage, and CountDeviationReason.

## FC-018 Direct Exe Gate

Must directly run critical executables; CTest alone is insufficient.

Default timeouts:

- local_file/source_core/runtime/evidence/no_debug: 10 seconds.
- HTTP range: 20 seconds.
- WebDAV bridge: 20 seconds.
- thread_safety: 30 seconds.
- max per executable: 120 seconds.

Required direct executables:

- `kivo_video_provider_local_file_read_test`
- `kivo_video_provider_local_file_evidence_test`
- `kivo_video_provider_local_file_direct_input_test`
- `kivo_video_provider_local_file_basic_thread_safety_test`
- `kivo_video_provider_http_range_open_test`
- `kivo_video_provider_http_range_read_test`
- `kivo_video_provider_http_range_positioned_read_test`
- `kivo_video_provider_http_range_seek_test`
- `kivo_video_provider_http_range_source_changed_test`
- `kivo_video_provider_http_range_auth_redaction_test`
- `kivo_video_provider_http_range_direct_input_test`
- `kivo_video_provider_webdav_open_test`
- `kivo_video_provider_webdav_range_read_test`
- `kivo_video_provider_webdav_direct_input_test`
- `kivo_video_no_debug_dialog_test`

For each executable record:

- resolved path.
- working directory.
- timeout seconds.
- stdout/stderr log paths.
- exit code.
- timeout YES/NO.
- Debug Error dialog YES/NO.
- abort popup YES/NO.
- Retry/Abort/Ignore popup YES/NO.
- secret leak YES/NO.

Output containing Debug Error, Retry/Abort/Ignore, assertion failure, terminate, abort, unhandled exception, access violation, raw Authorization/Cookie/Bearer/access_token/signed URL patterns is FAIL.

If no_debug test reports scanning zero files or only directories, STOP and investigate.

## FC-019 Evidence Schema Gate

Validate existing evidence:

- `artifacts/p2/evidence/LOCAL-FILE-SOURCE-RUNTIME-001.json`
- `artifacts/p2/evidence/REMOTE-RANGE-RUNTIME-HTTP-WEBDAV-001.json`

Prove validator supports `p2-evidence-v15`.

After generating closure evidence, validate:

- `artifacts/p2/evidence/P2-SOURCE-RUNTIME-FINAL-CLOSURE-001.json`

If current validator does not support closure custom fields:

- do not force unsupported fields.
- closure JSON uses the minimal legal structure supported by validator.
- full closure semantics move into the generated execution card.
- generated execution card must record validator schema downgrade strategy.

If validator supports metadata/notes/reference/minimal schema fields, use them. If not, keep JSON minimal and put full semantics in the execution card.

## FC-020 Redline Gate

Run:

- redline self-test.
- full redline scan from repository root.

Requirements:

- self-test PASS.
- full scan PASS.
- do not modify redline checker to let issues pass.

## FC-021 Extra Forbidden Scan Gate

Run and classify:

- provider_manager/source_manager scan.
- P3 pollution scan for FFmpeg, D3D11, DXGI, Direct3D, Vulkan, OpenGL, Metal, WASAPI, DXVA, demux, decoder, decode, renderer, render, Qt/QML/ImGui, cache, prefetch, benchmark.
- crash-risk/no-debug scan for assert/abort/terminate/debugbreak, `.at()`, `.value()`, numeric parsing APIs, regex, locale, codecvt.
- secret leak sentinel scan after closure evidence/card are generated.

Rules:

- production provider_manager/source_manager: FAIL.
- P3 ability in production implementation: FAIL.
- hits in tests, forbidden docs, allowlists, comments, sentinel strings require manual classification.
- string literal that becomes runtime capability claim: FAIL.
- `.at()`/`.value()` are not automatic FAIL, but must prove exceptions cannot escape into Debug Error Dialog.
- parser paths default-ban `std::stoull`, `std::stoi`, stringstream numeric parsing, regex, locale, codecvt unless a safe wrapper and tests prove safety.
- classification result must be written into the generated execution card.

## FC-022 Placeholder / Fabrication Gate

Use the narrow placeholder pattern. Do not use broad `<[^>]+>` because WebDAV XML and Markdown footnotes are valid.

Forbidden tokens include:

- `<hash>`
- `<git rev-parse HEAD>`
- `<resolved>`
- `<TODO>`
- `<TBD>`
- `<PLACEHOLDER>`
- `TODO_PLACEHOLDER`
- `TBD_PLACEHOLDER`
- `PLACEHOLDER_VALUE`
- `FIXME_PLACEHOLDER`
- `填入`
- `待填`
- `未填写`
- `假引用`
- `未替换模板值`

Do not false-positive legal WebDAV XML tags such as `<D:href>`, `<D:propfind>`, `<D:multistatus>`, `<D:response>`.

Do not false-positive normal Markdown footnotes such as `[^1]` or `[1]: ...`.

Secret redaction still applies even inside XML/Markdown.

## FC-023 WebDAV not_verified Anti-Forgery Gate

Must keep these as not_verified:

- WebDAV href exact-match: `not_verified`
- WebDAV collection rejection: `not_verified`
- WebDAV malformed XML fail-closed: `not_verified`
- WebDAV large XML body cap: `not_verified`

Forbidden substitutions:

- `verified`
- `PASS`
- `true`
- `complete`
- `done`

Failure classification:

`FAIL_P2_SOURCE_RUNTIME_FINAL_CLOSURE_WEBDAV_NOT_VERIFIED_FORGERY`

## FC-024 Final Closure Evidence JSON Gate

Generate:

`artifacts/p2/evidence/P2-SOURCE-RUNTIME-FINAL-CLOSURE-001.json`

Prefer current validator-supported schema. Try `p2-evidence-v15` only if validator supports it.

Never:

- modify validator just for pretty fields.
- delete key facts to pass schema.
- turn WebDAV not_verified into true/PASS/verified.
- claim loopback socket runtime verified.
- claim production transport complete.
- claim Emby/Alist/cloud/HLS/FFmpeg/decode/render verified.
- claim final DirectPlay/DirectStream decision verified.
- claim high-bitrate performance.
- submit unsupported fields.
- submit placeholders.
- submit real secrets, cookies, signed URLs, or access tokens.

Minimum semantics must be expressed either in JSON or, if schema cannot hold it, in generated execution card:

- task id.
- task family `source_runtime`.
- status PASS/closed equivalent allowed by validator.
- source runtime baseline verified.
- local_file/http_range/webdav_range_bridge baseline verified.
- test total 86 or matrix-equivalent.
- passed equals total, failed 0.
- no provider_manager/source_manager.
- no orphan branch.
- no unrelated histories.
- no P2 feature expansion.
- no P3 work.
- production transport not claimed.
- loopback socket runtime not_verified.
- not_claimed for Emby/Alist/cloud/HLS/FFmpeg/decode/render/HDR/WASAPI/final decisions/high bitrate.

## FC-025 Final Closure Execution Card Gate

Generate after real verification:

`docs/execution_cards/P2_SOURCE_RUNTIME_FINAL_CLOSURE_001.md`

Do not generate it early with fake PASS.

It must include:

- P2 closure summary.
- branch lineage proof.
- baseline commits.
- evidence freeze hash.
- diff scope result.
- test matrix.
- direct exe matrix.
- evidence JSON list.
- gate results.
- what P2 verifies.
- what P2 explicitly does not verify.
- Ready-for-P3 statement.
- P3 suggested entry points.
- minimal fixes section, even if none.
- validator schema note.
- no tag statement.
- WebDAV not_verified statement.
- closure commit count from `c90cfdc`.
- placeholder/fabrication result.
- direct exe timeout result.
- evidence hash before/after comparison.
- untracked file scope review.
- commit model A/B/C.
- execution start/end times.
- Git/CMake/CTest/Python/PowerShell versions.
- closure worktree path.
- original worktree before/after status.

Boundary statement must be explicit:

- closes P2 Source Runtime baseline only.
- verifies local_file runtime, HTTP range runtime, WebDAV range bridge.
- does not verify Emby/Alist/cloud/HLS/FFmpeg/decode/render/HDR/WASAPI/playback pipeline.
- does not verify DirectPlay/DirectStream final decisions.
- does not claim high bitrate performance.
- P3 may start after closure.

## FC-026 Untracked File Scope Gate

In closure worktree, list untracked files.

Default allowed untracked files:

- `artifacts/p2/evidence/P2-SOURCE-RUNTIME-FINAL-CLOSURE-001.json`
- `docs/execution_cards/P2_SOURCE_RUNTIME_FINAL_CLOSURE_001.md`

If bug fix adds files:

- they must be inside P2 source runtime allowlist.
- Repair Plan must record them.
- generated execution card must record them.
- they must enter diff scope review.

To show new files in diff, `git add -N` is allowed for closure artifacts and allowed bug fix files.

If review fails, undo intent-to-add only with `git reset -q -- <path>`.

Do not use:

- `git reset --hard`
- `git clean`
- `git restore .`

Unknown or unrelated files: STOP.

Build directories must not be `git add -N` staged.

## FC-027 Final Pre-Commit Gate

Before real staging, verify:

- `git status --short --untracked-files=all`
- `git diff --name-status`
- `git diff --stat`
- `git ls-files --others --exclude-standard`

Only allowed:

- closure evidence.
- closure execution card.
- necessary P2 source runtime bug fix files.

Not allowed:

- build artifacts.
- unrelated files.
- forbidden paths.

After `git add -N`, diff must show new files, paths must be allowlisted, and contents must pass placeholder, secret, and WebDAV anti-forgery gates.

Run closure evidence schema validation, redline full scan, evidence hash after compare, placeholder gate, secret gate, and WebDAV not_verified gate before real staging.

Failure classification:

`FAIL_P2_SOURCE_RUNTIME_FINAL_CLOSURE_PRE_COMMIT`

## FC-028 Staging Gate

Only after final pre-commit gate passes, real staging is allowed:

- stage final closure evidence.
- stage final closure execution card.
- stage allowed P2 source runtime bug fix files if any.

Then verify cached diff:

- contains only closure evidence/card and allowed bug fix files.
- contains no build artifacts.
- contains no unrelated files.
- contains no provider/P3/UI/render/FFmpeg/audio/cache/prefetch/benchmark pollution.
- does not modify validator/redline checker to bypass issues.

Failure classification:

`FAIL_P2_SOURCE_RUNTIME_FINAL_CLOSURE_STAGED_SCOPE`

## FC-029 Commit Gate

Recommended commit message:

`chore(source-runtime): close P2 source runtime baseline`

Recommended body must include:

- Task: `KIVO-VIDEO-P2-SOURCE-RUNTIME-FINAL-CLOSURE-001`
- closes P2 Source Runtime baseline.
- adds final closure evidence and execution card.
- verifies local_file runtime, HTTP range runtime, WebDAV range bridge.
- does not claim Emby/Alist/cloud/HLS/FFmpeg/decode/render/audio/HDR/WASAPI.
- keeps WebDAV profile completeness items not_verified.
- evidence paths.

After commit:

- commit succeeds.
- working tree clean.
- commit diff scope legal.
- default commit contains the two new closure artifacts.
- any extra files are allowed P2 source runtime bug fixes and documented.
- commit count matches model A/B/C.
- closure commit does not add provider.
- closure commit does not add P3 work.

## FC-030 Post-Commit Gate

After commit:

- validate closure evidence schema.
- run redline full scan.
- inspect commit files scope.
- confirm working tree clean.
- confirm closure branch commit count from `c90cfdc` matches model A/B/C.

All must pass before push.

## FC-031 Push Gate

Before push:

- branch must be `kivo-video-p2-source-runtime-final-closure-001`.
- working tree clean.
- closure commit files scope PASS.
- commit count model PASS.

Push:

`git push -u origin kivo-video-p2-source-runtime-final-closure-001`

After push:

- push succeeds.
- upstream set.
- working tree clean.
- remote branch points to local HEAD.
- record local and remote HEAD equality.

Do not force push.

## FC-032 No Tag Gate

Default:

- do not create tag.
- do not push tag.

Only if user explicitly requests a tag later, allowed tag name:

`p2-source-runtime-baseline-001`

Final report must say no new tag was created by this task.

## FC-033 Original Worktree Final Protection Gate

At the end, return to original worktree and read-only verify:

- branch.
- HEAD.
- status with untracked.
- last commit.

Requirements:

- original worktree HEAD unchanged by closure task.
- original branch unchanged by closure task.
- original worktree did not receive closure evidence/card.
- original worktree was not committed.
- original worktree was not pushed.

If original worktree state differs from start:

- STOP.
- Final Report marks `LOCAL_WORKTREE_CHANGED`.
- do not continue additional operations.

## FC-034 Bug Fix Policy Gate

Allowed bug fixes only:

- local_file / HTTP range / WebDAV range bridge existing P2 runtime behavior bugs.
- failures in current P2 runtime test matrix.
- direct exe gate failures.
- no debug dialog risks.
- closure evidence schema format issue.
- redline/forbidden scan true P2 pollution.
- secret redaction leak.
- existing baseline evidence mismatch with actual implementation.
- WebDAV not_verified false verified claim.
- mechanical issue blocking credible closure.

Not allowed:

- new Emby/Alist/cloud/HLS runtime.
- provider resolver/manager/source manager.
- FFmpeg probe, demux, decode, render, audio, HDR, WASAPI.
- cache/prefetch/benchmark.
- final DirectPlay/DirectStream decision.
- large refactor.
- flat file reorganization.
- unrelated module edits.
- weakening tests.
- weakening validator/redline checker.
- not_verified -> verified laundering.

Before bug fix, output Repair Plan:

- Bug.
- Failed Gate.
- Failed Command.
- Root Cause.
- Proposed Fix.
- Files To Change.
- Why P2 source runtime scope.
- Why not P3.
- Why not feature expansion.
- Why local project remains safe.

After bug fix, output Diff Review and include minimal fixes section in final artifacts.

If root cause or file scope is unclear, STOP.

If reset/clean/force/delete source is needed, STOP.

## FC-035 Final Report Gate

Final report must include:

- Classification: PASS or FAIL.
- start/end time and tool versions.
- original and closure worktree paths.
- branch, commit HEAD, push, working tree, tag.
- lineage proof.
- baseline commits.
- evidence hash before/after.
- diff scope.
- test matrix.
- direct exe matrix.
- all gates.
- evidence file list.
- minimal fixes, even if none.
- WebDAV not_verified statement.
- final boundary statement.
- P2 closure wording.

If fail, do not use PASS template. FAIL report must include:

- failed gate.
- failed command.
- observed output.
- original worktree status.
- closure worktree status.
- commit created YES/NO.
- push completed YES/NO.
- recovery recommendation.

Safe recovery recommendations only:

- keep closure worktree for inspection.
- provide diff/status to user.
- create another isolated worktree.
- user decides whether to delete failed worktree.
- do not recommend reset hard, clean, source deletion, or force push unless user separately approves after backup.

## FC-036 PASS Conditions Gate

Only PASS if all are true:

- isolated worktree used or in-place explicitly approved.
- original worktree not modified/switched/committed/pushed.
- closure start not orphan.
- c7f829 ancestor and c90cfdc exact baseline proven.
- c90cfdc commit message correct.
- origin repair branch points to c90cfdc.
- merge-base with base remote exists.
- diff scope legal.
- untracked files reviewed.
- staged scope legal.
- existing evidence hashes compared and any change legally recorded.
- fresh build dir and no stale exe.
- CTest matrix PASS.
- direct exe and timeout gates PASS.
- no debug dialog PASS.
- evidence schema PASS.
- redline PASS.
- forbidden token scan classified with no production pollution.
- secret redaction PASS.
- WebDAV not_verified anti-forgery PASS.
- placeholder/fabrication PASS and no false-positive XML/Markdown.
- final closure evidence generated.
- final closure execution card generated.
- committed artifacts have no placeholder/TODO/TBD/fake reference.
- any bug fix is legal, minimal, verified, and recorded.
- commit created.
- push successful.
- closure worktree clean.
- original worktree final status safe.
- no tag created.
- no new provider.
- no FFmpeg/probe/decode/render/UI/audio/cache/prefetch/benchmark.
- no WebDAV not_verified laundering.
- validator/redline checker not weakened.
- final report contains full lineage, commit model, tests, direct exe, timeout, environment, evidence, hash, gates, and boundary.

If any condition fails:

`Classification = FAIL_P2_SOURCE_RUNTIME_FINAL_CLOSURE_001`

## FC-037 Post-Closure Stage Wording Gate

After PASS, future wording is:

- `P2 closed.`
- `P2 does not add more source runtime features.`
- `P3 can start.`

P3 must not be called a P2 child task.

Suggested P3 entry points:

- `KIVO-VIDEO-P3-PROVIDER-INTEGRATION-PLANNING-001`
- `KIVO-VIDEO-P3-MEDIA-PROBE-PLANNING-001`
- `KIVO-VIDEO-P3-PLAYBACK-PIPELINE-PLANNING-001`

The user decides which P3 entry point comes next.

## FC-038 Final Output Gate

Final successful output must include exactly the closure meaning:

- `P2 closed.`
- `P2 source runtime baseline locked.`
- `Ready for P3 planning / P3 implementation.`

Do not add broader product claims.
